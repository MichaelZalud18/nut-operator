apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
    control-plane: controller-manager
  name: nut-operator-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: nodepoweragents.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: NodePowerAgent
    listKind: NodePowerAgentList
    plural: nodepoweragents
    singular: nodepoweragent
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodePowerAgent is the Schema for the nodepoweragents API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodePowerAgent
            properties:
              hardening:
                description: hardening overrides pod hardening defaults.
                properties:
                  readOnlyRootFilesystem:
                    default: true
                    description: readOnlyRootFilesystem requests read-only container
                      root filesystems.
                    type: boolean
                  runAsNonRoot:
                    default: true
                    description: runAsNonRoot requests non-root execution where the
                      operand supports it.
                    type: boolean
                  seccompProfileType:
                    default: RuntimeDefault
                    description: seccompProfileType is the seccomp profile type for
                      generated pods.
                    enum:
                    - RuntimeDefault
                    - Localhost
                    - Unconfined
                    type: string
                type: object
              images:
                description: images overrides the default agent images.
                properties:
                  actuator:
                    description: |-
                      actuator is the host-actuation image. Stub mode runs without host access; real host
                      shutdown is rendered only after explicit actuation approval.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  upsmon:
                    description: upsmon is the unprivileged NUT client image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                type: object
              managementClusterRef:
                description: managementClusterRef references the owning PowerManagementCluster.
                properties:
                  name:
                    description: name is the referenced object's metadata.name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              mode:
                default: DryRun
                description: mode controls whether the agent only observes, dry-runs,
                  or actuates host shutdown.
                enum:
                - MonitorOnly
                - DryRun
                - Actuate
                type: string
              namespace:
                description: namespace overrides the operand namespace for this agent
                  fleet.
                type: string
              nodeSelector:
                description: nodeSelector selects nodes managed by this agent fleet.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              nutServerRefs:
                description: nutServerRefs names NUTServer resources monitored by
                  this agent fleet.
                items:
                  description: ObjectNameReference references another cluster-scoped
                    power.zalud.io object by name.
                  properties:
                    name:
                      description: name is the referenced object's metadata.name.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              placement:
                description: placement controls DaemonSet scheduling.
                properties:
                  affinity:
                    description: affinity controls pod scheduling affinity and anti-affinity.
                    properties:
                      nodeAffinity:
                        description: Describes node affinity scheduling rules for
                          the pod.
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node matches the corresponding matchExpressions; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: |-
                                An empty preferred scheduling term matches all objects with implicit weight 0
                                (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                              properties:
                                preference:
                                  description: A node selector term, associated with
                                    the corresponding weight.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                weight:
                                  description: Weight associated with matching the
                                    corresponding nodeSelectorTerm, in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - preference
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to an update), the system
                              may or may not try to eventually evict the pod from its node.
                            properties:
                              nodeSelectorTerms:
                                description: Required. A list of node selector terms.
                                  The terms are ORed.
                                items:
                                  description: |-
                                    A null or empty node selector term matches no objects. The requirements of
                                    them are ANDed.
                                    The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - nodeSelectorTerms
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      podAffinity:
                        description: Describes pod affinity scheduling rules (e.g.
                          co-locate this pod in the same node, zone, etc. as some
                          other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      podAntiAffinity:
                        description: Describes pod anti-affinity scheduling rules
                          (e.g. avoid putting this pod in the same node, zone, etc.
                          as some other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the anti-affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling anti-affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and subtracting
                              "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the anti-affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the anti-affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector constrains operand pods to nodes with
                      matching labels.
                    type: object
                  priorityClassName:
                    description: priorityClassName sets the operand pod priority class.
                    type: string
                  tolerations:
                    description: tolerations allows operand pods to schedule onto
                      tainted nodes.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              resources:
                description: resources configures agent container requests and limits.
                properties:
                  actuator:
                    description: actuator configures the actuator container resources.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  upsmon:
                    description: upsmon configures the NUT client container resources.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              shutdown:
                description: shutdown configures the handoff signal and actuator behavior.
                properties:
                  actuatorPolicy:
                    default: Stub
                    description: actuatorPolicy chooses disabled, stubbed, or real
                      systemd host poweroff.
                    enum:
                    - Disabled
                    - Stub
                    - SystemdPoweroff
                    type: string
                  approvalAnnotation:
                    description: approvalAnnotation must be present on this NodePowerAgent
                      before SystemdPoweroff is rendered.
                    type: string
                  requireFreshTelemetry:
                    default: true
                    description: requireFreshTelemetry blocks actuation when UPS telemetry
                      is stale.
                    type: boolean
                  signalPath:
                    default: /run/power-agent/shutdown.json
                    description: signalPath is the shared in-pod file path written
                      by upsmon and watched by the actuator.
                    type: string
                  signalTTL:
                    description: signalTTL rejects stale signal files older than this
                      duration.
                    type: string
                type: object
              shutdownFlowRef:
                description: shutdownFlowRef references the flow compiled into this
                  agent fleet.
                properties:
                  name:
                    description: name is the referenced object's metadata.name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              upsmon:
                description: upsmon configures generated upsmon.conf values.
                properties:
                  alertPollFrequency:
                    description: alertPollFrequency sets POLLFREQALERT.
                    type: string
                  deadTime:
                    description: deadTime sets DEADTIME.
                    type: string
                  finalDelay:
                    description: finalDelay sets FINALDELAY.
                    type: string
                  hostSync:
                    description: hostSync sets HOSTSYNC.
                    type: string
                  pollFrequency:
                    description: pollFrequency sets POLLFREQ.
                    type: string
                type: object
            required:
            - nutServerRefs
            type: object
          status:
            description: status defines the observed state of NodePowerAgent
            properties:
              conditions:
                description: conditions represent the current state of the NodePowerAgent
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: configHash identifies the rendered agent configuration.
                type: string
              desiredNumberScheduled:
                description: desiredNumberScheduled mirrors the generated DaemonSet
                  status.
                format: int32
                type: integer
              managedResources:
                description: managedResources lists resources managed for this agent
                  fleet.
                items:
                  description: ManagedResourceStatus records one Kubernetes resource
                    managed for a CR.
                  properties:
                    apiVersion:
                      description: apiVersion is the managed resource apiVersion.
                      type: string
                    hash:
                      description: hash is the rendered configuration hash, when applicable.
                      type: string
                    kind:
                      description: kind is the managed resource kind.
                      type: string
                    name:
                      description: name is the managed resource name.
                      type: string
                    namespace:
                      description: namespace is the managed resource namespace, when
                        namespaced.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              nodeStatuses:
                description: |-
                  nodeStatuses records per-node agent pod coverage observed from Kubernetes pod readiness.
                  It is the Kubernetes-native node-agent heartbeat used by shutdown execution safety gates.
                items:
                  description: NodePowerAgentNodeStatus records controller-observed
                    agent coverage for one selected node.
                  properties:
                    lastHeartbeatTime:
                      description: lastHeartbeatTime is the latest pod readiness transition
                        observed by the controller.
                      format: date-time
                      type: string
                    message:
                      description: message explains the observed coverage state for
                        operators and audit records.
                      type: string
                    nodeName:
                      description: nodeName is the Kubernetes Node name selected by
                        this NodePowerAgent.
                      type: string
                    phase:
                      description: phase is the observed pod phase, when present.
                      type: string
                    podName:
                      description: podName is the observed agent pod on this node,
                        when present.
                      type: string
                    ready:
                      description: ready is true when a current, ready NodePowerAgent
                        pod is observed on this node.
                      type: boolean
                    reason:
                      description: reason is a machine-readable coverage reason such
                        as AgentPodReady or AgentPodMissing.
                      type: string
                  required:
                  - nodeName
                  - ready
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - nodeName
                x-kubernetes-list-type: map
              numberReady:
                description: numberReady mirrors the generated DaemonSet status.
                format: int32
                type: integer
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes agent readiness.
                enum:
                - Pending
                - Rendering
                - Ready
                - Degraded
                - Error
                type: string
              readyNodeCount:
                description: readyNodeCount records selected nodes with an observed
                  ready agent pod.
                format: int32
                type: integer
              selectedNodes:
                description: selectedNodes lists nodes selected by this fleet at the
                  last reconcile.
                items:
                  type: string
                type: array
              unavailableNodeCount:
                description: unavailableNodeCount records selected nodes without an
                  observed ready agent pod.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: nutservers.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: NUTServer
    listKind: NUTServerList
    plural: nutservers
    singular: nutserver
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NUTServer is the Schema for the nutservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NUTServer
            properties:
              auth:
                description: auth configures NUT users and credential management.
                properties:
                  existingSecretRef:
                    description: existingSecretRef points at an upsd.users-compatible
                      Secret when mode is ExistingSecret.
                    properties:
                      name:
                        description: name is the referenced object's metadata.name.
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the referenced object's namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  mode:
                    default: OperatorManaged
                    description: mode selects whether the operator generates NUT users
                      or consumes an existing Secret.
                    enum:
                    - OperatorManaged
                    - ExistingSecret
                    type: string
                  perNodeUsers:
                    default: true
                    description: perNodeUsers requests distinct upsmon secondary credentials
                      for each node.
                    type: boolean
                type: object
              config:
                description: config tunes generated upsd and driver configuration.
                properties:
                  listenAddress:
                    default: 0.0.0.0
                    description: listenAddress is the address rendered into upsd.conf.
                    type: string
                  maxStartDelay:
                    description: maxStartDelay configures maxstartdelay in ups.conf.
                    type: string
                  pollInterval:
                    description: pollInterval sets the default driver poll interval.
                    type: string
                type: object
              deviceRefs:
                description: deviceRefs names UPSDevice resources served by this upsd
                  instance.
                items:
                  description: ObjectNameReference references another cluster-scoped
                    power.zalud.io object by name.
                  properties:
                    name:
                      description: name is the referenced object's metadata.name.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              deviceSelector:
                description: deviceSelector selects UPSDevice resources served by
                  this upsd instance.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              image:
                description: image overrides the default NUT server image.
                properties:
                  digest:
                    description: digest is an optional immutable image digest.
                    type: string
                  pullPolicy:
                    description: pullPolicy controls when the kubelet pulls the image.
                    type: string
                  repository:
                    description: repository is the image repository, without the tag.
                    type: string
                  tag:
                    description: tag is the image tag. Production installs should
                      prefer digest pinning in packaging overlays.
                    type: string
                type: object
              managementClusterRef:
                description: managementClusterRef references the owning PowerManagementCluster.
                properties:
                  name:
                    description: name is the referenced object's metadata.name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              namespace:
                description: namespace overrides the operand namespace for this server.
                type: string
              placement:
                description: placement controls where the server runs.
                properties:
                  affinity:
                    description: affinity controls pod scheduling affinity and anti-affinity.
                    properties:
                      nodeAffinity:
                        description: Describes node affinity scheduling rules for
                          the pod.
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node matches the corresponding matchExpressions; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: |-
                                An empty preferred scheduling term matches all objects with implicit weight 0
                                (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).
                              properties:
                                preference:
                                  description: A node selector term, associated with
                                    the corresponding weight.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                weight:
                                  description: Weight associated with matching the
                                    corresponding nodeSelectorTerm, in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - preference
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to an update), the system
                              may or may not try to eventually evict the pod from its node.
                            properties:
                              nodeSelectorTerms:
                                description: Required. A list of node selector terms.
                                  The terms are ORed.
                                items:
                                  description: |-
                                    A null or empty node selector term matches no objects. The requirements of
                                    them are ANDed.
                                    The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.
                                  properties:
                                    matchExpressions:
                                      description: A list of node selector requirements
                                        by node's labels.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchFields:
                                      description: A list of node selector requirements
                                        by node's fields.
                                      items:
                                        description: |-
                                          A node selector requirement is a selector that contains values, a key, and an operator
                                          that relates the key and values.
                                        properties:
                                          key:
                                            description: The label key that the selector
                                              applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              Represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
                                            type: string
                                          values:
                                            description: |-
                                              An array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. If the operator is Gt or Lt, the values
                                              array must have a single element, which will be interpreted as an integer.
                                              This array is replaced during a strategic merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                  x-kubernetes-map-type: atomic
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - nodeSelectorTerms
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      podAffinity:
                        description: Describes pod affinity scheduling rules (e.g.
                          co-locate this pod in the same node, zone, etc. as some
                          other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and adding
                              "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      podAntiAffinity:
                        description: Describes pod anti-affinity scheduling rules
                          (e.g. avoid putting this pod in the same node, zone, etc.
                          as some other pod(s)).
                        properties:
                          preferredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              The scheduler will prefer to schedule pods to nodes that satisfy
                              the anti-affinity expressions specified by this field, but it may choose
                              a node that violates one or more of the expressions. The node that is
                              most preferred is the one with the greatest sum of weights, i.e.
                              for each node that meets all of the scheduling requirements (resource
                              request, requiredDuringScheduling anti-affinity expressions, etc.),
                              compute a sum by iterating through the elements of this field and subtracting
                              "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
                              node(s) with the highest sum are the most preferred.
                            items:
                              description: The weights of all of the matched WeightedPodAffinityTerm
                                fields are added per-node to find the most preferred
                                node(s)
                              properties:
                                podAffinityTerm:
                                  description: Required. A pod affinity term, associated
                                    with the corresponding weight.
                                  properties:
                                    labelSelector:
                                      description: |-
                                        A label query over a set of resources, in this case pods.
                                        If it's null, this PodAffinityTerm matches with no Pods.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    matchLabelKeys:
                                      description: |-
                                        MatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                        Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    mismatchLabelKeys:
                                      description: |-
                                        MismatchLabelKeys is a set of pod label keys to select which pods will
                                        be taken into consideration. The keys are used to lookup values from the
                                        incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                        to select the group of existing pods which pods will be taken into consideration
                                        for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                        pod labels will be ignored. The default value is empty.
                                        The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                        Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    namespaceSelector:
                                      description: |-
                                        A label query over the set of namespaces that the term applies to.
                                        The term is applied to the union of the namespaces selected by this field
                                        and the ones listed in the namespaces field.
                                        null selector and null or empty namespaces list means "this pod's namespace".
                                        An empty selector ({}) matches all namespaces.
                                      properties:
                                        matchExpressions:
                                          description: matchExpressions is a list
                                            of label selector requirements. The requirements
                                            are ANDed.
                                          items:
                                            description: |-
                                              A label selector requirement is a selector that contains values, a key, and an operator that
                                              relates the key and values.
                                            properties:
                                              key:
                                                description: key is the label key
                                                  that the selector applies to.
                                                type: string
                                              operator:
                                                description: |-
                                                  operator represents a key's relationship to a set of values.
                                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                                type: string
                                              values:
                                                description: |-
                                                  values is an array of string values. If the operator is In or NotIn,
                                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                  the values array must be empty. This array is replaced during a strategic
                                                  merge patch.
                                                items:
                                                  type: string
                                                type: array
                                                x-kubernetes-list-type: atomic
                                            required:
                                            - key
                                            - operator
                                            type: object
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: |-
                                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                                          type: object
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    namespaces:
                                      description: |-
                                        namespaces specifies a static list of namespace names that the term applies to.
                                        The term is applied to the union of the namespaces listed in this field
                                        and the ones selected by namespaceSelector.
                                        null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    topologyKey:
                                      description: |-
                                        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                        the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                        whose value of the label with key topologyKey matches that of any node on which any of the
                                        selected pods is running.
                                        Empty topologyKey is not allowed.
                                      type: string
                                  required:
                                  - topologyKey
                                  type: object
                                weight:
                                  description: |-
                                    weight associated with matching the corresponding podAffinityTerm,
                                    in the range 1-100.
                                  format: int32
                                  type: integer
                              required:
                              - podAffinityTerm
                              - weight
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          requiredDuringSchedulingIgnoredDuringExecution:
                            description: |-
                              If the anti-affinity requirements specified by this field are not met at
                              scheduling time, the pod will not be scheduled onto the node.
                              If the anti-affinity requirements specified by this field cease to be met
                              at some point during pod execution (e.g. due to a pod label update), the
                              system may or may not try to eventually evict the pod from its node.
                              When there are multiple elements, the lists of nodes corresponding to each
                              podAffinityTerm are intersected, i.e. all terms must be satisfied.
                            items:
                              description: |-
                                Defines a set of pods (namely those matching the labelSelector
                                relative to the given namespace(s)) that this pod should be
                                co-located (affinity) or not co-located (anti-affinity) with,
                                where co-located is defined as running on a node whose value of
                                the label with key <topologyKey> matches that of any node on which
                                a pod of the set of pods is running
                              properties:
                                labelSelector:
                                  description: |-
                                    A label query over a set of resources, in this case pods.
                                    If it's null, this PodAffinityTerm matches with no Pods.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                matchLabelKeys:
                                  description: |-
                                    MatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both matchLabelKeys and labelSelector.
                                    Also, matchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                mismatchLabelKeys:
                                  description: |-
                                    MismatchLabelKeys is a set of pod label keys to select which pods will
                                    be taken into consideration. The keys are used to lookup values from the
                                    incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
                                    to select the group of existing pods which pods will be taken into consideration
                                    for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
                                    pod labels will be ignored. The default value is empty.
                                    The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
                                    Also, mismatchLabelKeys cannot be set when labelSelector isn't set.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                namespaceSelector:
                                  description: |-
                                    A label query over the set of namespaces that the term applies to.
                                    The term is applied to the union of the namespaces selected by this field
                                    and the ones listed in the namespaces field.
                                    null selector and null or empty namespaces list means "this pod's namespace".
                                    An empty selector ({}) matches all namespaces.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: |-
                                    namespaces specifies a static list of namespace names that the term applies to.
                                    The term is applied to the union of the namespaces listed in this field
                                    and the ones selected by namespaceSelector.
                                    null or empty namespaces list and null namespaceSelector means "this pod's namespace".
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                topologyKey:
                                  description: |-
                                    This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
                                    the labelSelector in the specified namespaces, where co-located is defined as running on a node
                                    whose value of the label with key topologyKey matches that of any node on which any of the
                                    selected pods is running.
                                    Empty topologyKey is not allowed.
                                  type: string
                              required:
                              - topologyKey
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector constrains operand pods to nodes with
                      matching labels.
                    type: object
                  priorityClassName:
                    description: priorityClassName sets the operand pod priority class.
                    type: string
                  tolerations:
                    description: tolerations allows operand pods to schedule onto
                      tainted nodes.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              replicas:
                default: 1
                description: |-
                  replicas is the number of upsd pods for network-backed UPS devices. Pinned to 1: multiple
                  replicas behind one Service split NUT client-login accounting across independent driver
                  instances, which breaks upsd's own tracking of when all clients have disconnected.
                format: int32
                maximum: 1
                minimum: 1
                type: integer
              resources:
                description: resources configures NUT server pod requests and limits.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              service:
                description: service configures the upsd Service.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: annotations are copied to the generated Service.
                    type: object
                  port:
                    default: 3493
                    description: port is the upsd service port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  type:
                    default: ClusterIP
                    description: type is the Kubernetes Service type.
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                type: object
              tls:
                description: tls configures NUT protocol TLS.
                properties:
                  clientCARef:
                    description: |-
                      clientCARef points at the CA Secret upsd uses to validate client certificates. It is
                      rendered as CERTPATH in upsd.conf and only consulted when verifyClientCertificates is
                      true. The Secret must carry data["ca.crt"].
                    properties:
                      name:
                        description: name is the referenced object's metadata.name.
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the referenced object's namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  disableWeakProtocols:
                    default: true
                    description: |-
                      disableWeakProtocols renders DISABLE_WEAK_SSL into upsd.conf, raising the minimum
                      negotiated version from TLS 1.0 to TLS 1.2.
                    type: boolean
                  mode:
                    default: Required
                    description: mode selects TLS enforcement.
                    enum:
                    - Disabled
                    - Opportunistic
                    - Required
                    type: string
                  serverCARef:
                    description: |-
                      serverCARef points at the CA bundle that NUT clients use to verify this server's
                      certificate. It is rendered as CERTPATH in upsmon.conf on every NodePowerAgent that
                      monitors this server. Without it upsmon can encrypt but cannot authenticate the server,
                      so CERTVERIFY stays off. The Secret must carry data["ca.crt"].
                    properties:
                      name:
                        description: name is the referenced object's metadata.name.
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the referenced object's namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  serverCertificateRef:
                    description: |-
                      serverCertificateRef points at the server certificate Secret. The Secret must be
                      kubernetes.io/tls shaped: data["tls.crt"] and data["tls.key"]. upsd's CERTFILE wants a
                      single PEM holding the chain followed by the key, so the operand assembles that file at
                      startup rather than requiring a pre-combined Secret key.
                    properties:
                      name:
                        description: name is the referenced object's metadata.name.
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the referenced object's namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  verifyClientCertificates:
                    default: false
                    description: |-
                      verifyClientCertificates renders CERTREQUEST into upsd.conf, requiring every NUT client to
                      present a certificate signed by clientCARef.

                      This defaults to false because the operator does not issue client certificates to upsmon.
                      Turning it on locks out every NodePowerAgent unless the agent image is NUT 2.8.6 or newer
                      (the first release where upsmon.conf accepts CERTFILE under OpenSSL) and an operator-external
                      mechanism supplies each agent a client certificate. CERTREQUEST is also a no-op on OpenSSL
                      upsd builds older than 2.8.6, so enabling it there silently grants no protection.
                    type: boolean
                type: object
            type: object
          status:
            description: status defines the observed state of NUTServer
            properties:
              conditions:
                description: conditions represent the current state of the NUTServer
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: configHash identifies the rendered NUT configuration.
                type: string
              managedResources:
                description: managedResources lists resources managed for this server.
                items:
                  description: ManagedResourceStatus records one Kubernetes resource
                    managed for a CR.
                  properties:
                    apiVersion:
                      description: apiVersion is the managed resource apiVersion.
                      type: string
                    hash:
                      description: hash is the rendered configuration hash, when applicable.
                      type: string
                    kind:
                      description: kind is the managed resource kind.
                      type: string
                    name:
                      description: name is the managed resource name.
                      type: string
                    namespace:
                      description: namespace is the managed resource namespace, when
                        namespaced.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes the server lifecycle.
                enum:
                - Pending
                - Rendering
                - Ready
                - Degraded
                - Error
                type: string
              readyReplicas:
                description: readyReplicas is the number of ready server pods.
                format: int32
                type: integer
              selectedDevices:
                description: selectedDevices names UPSDevice resources rendered into
                  this server.
                items:
                  type: string
                type: array
              serviceEndpoints:
                description: serviceEndpoints reports generated service endpoints.
                items:
                  description: ServiceEndpointStatus reports a reachable endpoint
                    produced by a controller.
                  properties:
                    dnsName:
                      description: dnsName is the in-cluster DNS name.
                      type: string
                    name:
                      description: name is the logical endpoint name.
                      type: string
                    namespace:
                      description: namespace is the Service namespace.
                      type: string
                    port:
                      description: port is the service port.
                      format: int32
                      type: integer
                  required:
                  - name
                  type: object
                type: array
              upstreamNUT:
                description: upstreamNUT reports selected upstream NUT devices and
                  their last TCP probe result.
                items:
                  description: NUTUpstreamStatus reports the rendered relay target
                    for one upstream NUT device.
                  properties:
                    authMode:
                      description: authMode is the authconf mode rendered for dummy-ups
                        repeater mode.
                      enum:
                      - None
                      - Default
                      - Secret
                      type: string
                    host:
                      description: host is the upstream upsd host.
                      type: string
                    lastProbeTime:
                      description: lastProbeTime is when reachable was last observed.
                      format: date-time
                      type: string
                    message:
                      description: message summarizes the most recent upstream probe.
                      type: string
                    nutName:
                      description: nutName is the local NUT name exposed by this NUTServer.
                      type: string
                    port:
                      description: port is the upstream upsd TCP port.
                      format: int32
                      type: integer
                    reachable:
                      description: reachable is the result of the most recent TCP
                        reachability probe.
                      type: boolean
                    strictStart:
                      description: strictStart reports whether upstream availability
                        is fatal at driver startup.
                      type: boolean
                    upsDevice:
                      description: upsDevice is the selected UPSDevice resource name.
                      type: string
                    upsName:
                      description: upsName is the upstream UPS name on the upstream
                        upsd server.
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: pducapabilityprofiles.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: PDUCapabilityProfile
    listKind: PDUCapabilityProfileList
    plural: pducapabilityprofiles
    singular: pducapabilityprofile
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PDUCapabilityProfile is the Schema for the pducapabilityprofiles
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PDUCapabilityProfile
            properties:
              outlets:
                description: outlets describes the outlet layout of matching devices.
                properties:
                  count:
                    description: |-
                      count is the number of outlets the device exposes. Unset means undeclared,
                      which is not the same as a device with no outlets; nothing infers one from
                      the other.
                    format: int32
                    minimum: 0
                    type: integer
                  switchable:
                    description: |-
                      switchable names the outlets that can be switched independently, using NUT's
                      outlet identifiers. Empty alongside a non-zero count describes a metered-only
                      PDU: it reports per-outlet readings and switches nothing.

                      Declaring an outlet here does not make it switchable by this operator. No PDU
                      actuation is implemented; see docs/tasks-post-v1.md.
                    items:
                      type: string
                    type: array
                type: object
              quirks:
                description: quirks records behavior-affecting quirks consumed by
                  planner or executor policy.
                items:
                  type: string
                type: array
              selector:
                description: |-
                  selector declares which PDU devices this profile can match. The precedence
                  chain is the one UPS profiles use: exact model plus firmware, exact model,
                  model glob, driver family, then the universal fallback.
                properties:
                  driverFamily:
                    description: driverFamily matches the NUT driver family, such
                      as snmp-ups.
                    type: string
                  firmware:
                    description: firmware narrows an exact model match to a specific
                      firmware string.
                    type: string
                  model:
                    description: model matches an exact provider model string.
                    type: string
                  modelGlob:
                    description: modelGlob matches a provider model string using path.Match
                      glob semantics.
                    type: string
                  universal:
                    default: false
                    description: |-
                      universal marks this profile as the unidentified-device fallback. At most one
                      PDU profile may set it.
                    type: boolean
                type: object
              telemetry:
                description: telemetry declares authoritative NUT variables exposed
                  by matching devices.
                properties:
                  aliases:
                    additionalProperties:
                      type: string
                    description: |-
                      aliases map a non-standard variable name the device reports (the key) onto
                      the canonical NUT name it should be read as (the value), with the same
                      one-directional, native-wins semantics as UPS profiles (OD-23).
                    type: object
                  variables:
                    description: variables are authoritative NUT variable names exposed
                      by matching devices.
                    items:
                      type: string
                    type: array
                type: object
              version:
                description: version is the semantic version of this behavior profile.
                minLength: 1
                type: string
            required:
            - selector
            - version
            type: object
          status:
            description: status defines the observed state of PDUCapabilityProfile
            properties:
              conditions:
                description: conditions represent the current state of the PDUCapabilityProfile
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes profile contract acceptance.
                enum:
                - Accepted
                - Error
                type: string
              profileHash:
                description: profileHash identifies the accepted capability profile
                  content.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: powerinfrastructures.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: PowerInfrastructure
    listKind: PowerInfrastructureList
    plural: powerinfrastructures
    singular: powerinfrastructure
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PowerInfrastructure is the Schema for the powerinfrastructures
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PowerInfrastructure
            properties:
              class:
                description: class describes the broad infrastructure role for operator
                  review.
                enum:
                - PDU
                - Switch
                - Router
                - TransferSwitch
                - PowerPanel
                - NetworkDevice
                - Other
                type: string
              description:
                description: description explains why this entity is relevant to power
                  planning.
                type: string
              displayName:
                description: displayName is a human-readable label for events and
                  published artifacts.
                type: string
            type: object
          status:
            description: status defines the observed state of PowerInfrastructure
            properties:
              conditions:
                description: conditions represent the current state of the PowerInfrastructure
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes inventory contract acceptance.
                enum:
                - Accepted
                - Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: powerinventoryedges.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: PowerInventoryEdge
    listKind: PowerInventoryEdgeList
    plural: powerinventoryedges
    singular: powerinventoryedge
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PowerInventoryEdge is the Schema for the powerinventoryedges
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PowerInventoryEdge
            properties:
              description:
                description: description explains the modeled dependency for operator
                  review.
                type: string
              from:
                description: from is the source entity for this relation.
                properties:
                  kind:
                    description: kind is the referenced inventory entity kind.
                    enum:
                    - UPSDevice
                    - Node
                    - PowerInfrastructure
                    type: string
                  name:
                    description: name is the referenced UPSDevice, PowerInfrastructure,
                      or Kubernetes Node name.
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
              input:
                description: input identifies the target power input for feeds edges.
                type: string
              relation:
                description: relation is the topology relation.
                enum:
                - Feeds
                - Carries
                type: string
              to:
                description: to is the target entity for this relation.
                properties:
                  kind:
                    description: kind is the referenced inventory entity kind.
                    enum:
                    - UPSDevice
                    - Node
                    - PowerInfrastructure
                    type: string
                  name:
                    description: name is the referenced UPSDevice, PowerInfrastructure,
                      or Kubernetes Node name.
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - from
            - relation
            - to
            type: object
          status:
            description: status defines the observed state of PowerInventoryEdge
            properties:
              conditions:
                description: conditions represent the current state of the PowerInventoryEdge
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes inventory contract acceptance.
                enum:
                - Accepted
                - Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: powerinventorynodes.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: PowerInventoryNode
    listKind: PowerInventoryNodeList
    plural: powerinventorynodes
    singular: powerinventorynode
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PowerInventoryNode is the Schema for the powerinventorynodes
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PowerInventoryNode
            properties:
              communicationPathExempt:
                default: false
                description: communicationPathExempt suppresses missing carries-path
                  warnings for this node.
                type: boolean
              nodeName:
                description: nodeName is the canonical Kubernetes Node metadata.name
                  this inventory record describes.
                minLength: 1
                type: string
              powerPlanningExempt:
                default: false
                description: powerPlanningExempt excludes this node from the UPS feeds
                  orphan rule.
                type: boolean
              roles:
                description: roles declares planner-relevant node roles.
                properties:
                  controlPlane:
                    default: false
                    description: controlPlane marks this node as hosting Kubernetes
                      control-plane responsibilities.
                    type: boolean
                  controlPlaneQuorumMember:
                    default: false
                    description: controlPlaneQuorumMember marks this node as counting
                      toward control-plane quorum.
                    type: boolean
                  lastDitchRole:
                    description: |-
                      lastDitchRole is an explicit late-shutdown role interpreted by planner policy.
                      When shutdownTier is omitted, any non-empty lastDitchRole maps this node to tier 1.
                    type: string
                  shutdownTier:
                    description: shutdownTier explicitly assigns this node to a numbered
                      shutdown tier. Nodes cannot use tier 0.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
            required:
            - nodeName
            type: object
          status:
            description: status defines the observed state of PowerInventoryNode
            properties:
              conditions:
                description: conditions represent the current state of the PowerInventoryNode
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes inventory contract acceptance.
                enum:
                - Accepted
                - Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: powermanagementclusters.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: PowerManagementCluster
    listKind: PowerManagementClusterList
    plural: powermanagementclusters
    singular: powermanagementcluster
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PowerManagementCluster is the Schema for the powermanagementclusters
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PowerManagementCluster
            properties:
              images:
                description: images provides default images for generated operands.
                properties:
                  actuator:
                    description: actuator is the minimal node-actuation image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  exporter:
                    description: exporter is the metrics exporter image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  nutServer:
                    description: nutServer is the upsd and driver image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  operator:
                    description: operator is the controller-manager image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  upsmonAgent:
                    description: upsmonAgent is the unprivileged NUT client image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                type: object
              inventory:
                description: inventory configures how declarative inventory snapshots
                  are treated.
                properties:
                  snapshotAgeLevels:
                    description: |-
                      snapshotAgeLevels raise the reported severity as an inventory snapshot ages
                      (IN-16). Age never blocks planning: a provider outage must degrade rather
                      than stop a shutdown (IN-15), so the worst an old snapshot can do is say so
                      loudly. The levels exist because "last good snapshot" must not be able to
                      quietly mean "eight months ago".

                      Thresholds are evaluated highest-first, so the oldest matching entry wins.
                      An empty list applies the defaults: Info at one hour, Warning at six.
                    items:
                      description: PowerSnapshotAgeLevel raises one severity once
                        a snapshot reaches an age.
                      properties:
                        after:
                          description: after is the snapshot age at which this level
                            begins to apply.
                          pattern: ^([0-9]+(\.[0-9]+)?(ns|us|ms|s|m|h))+$
                          type: string
                        level:
                          description: |-
                            level is the severity reported once a snapshot is older than after. Info is
                            recorded and published without changing conditions; Warning additionally
                            degrades flows compiled from the snapshot.
                          enum:
                          - Info
                          - Warning
                          type: string
                      required:
                      - after
                      - level
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - level
                    x-kubernetes-list-type: map
                type: object
              observability:
                description: observability configures metrics, events, and telemetry
                  export defaults.
                properties:
                  kubernetesEvents:
                    default: true
                    description: kubernetesEvents enables Kubernetes event emission
                      for power state changes.
                    type: boolean
                  serviceMonitor:
                    default: true
                    description: serviceMonitor enables Prometheus Operator ServiceMonitor
                      rendering.
                    type: boolean
                  telemetryInterval:
                    description: telemetryInterval is the default interval for durable
                      UPS telemetry snapshots.
                    type: string
                type: object
              operandNamespace:
                description: operandNamespace defines where generated Deployments,
                  DaemonSets, Services, ConfigMaps, and Secrets live.
                properties:
                  create:
                    default: true
                    description: create allows the operator packaging to create the
                      namespace.
                    type: boolean
                  name:
                    default: power-system
                    description: name is the namespace for generated workloads and
                      Services.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              security:
                description: security sets global security defaults for generated
                  operands.
                properties:
                  allowedActuatorNamespaces:
                    description: allowedActuatorNamespaces restricts where host-actuator
                      DaemonSets may be created.
                    items:
                      type: string
                    type: array
                  defaultPodHardening:
                    description: defaultPodHardening applies to generated non-actuator
                      containers.
                    properties:
                      readOnlyRootFilesystem:
                        default: true
                        description: readOnlyRootFilesystem requests read-only container
                          root filesystems.
                        type: boolean
                      runAsNonRoot:
                        default: true
                        description: runAsNonRoot requests non-root execution where
                          the operand supports it.
                        type: boolean
                      seccompProfileType:
                        default: RuntimeDefault
                        description: seccompProfileType is the seccomp profile type
                          for generated pods.
                        enum:
                        - RuntimeDefault
                        - Localhost
                        - Unconfined
                        type: string
                    type: object
                  profile:
                    default: Restricted
                    description: profile selects the operator's workload hardening
                      baseline.
                    enum:
                    - Restricted
                    - HostActuatorIsolated
                    type: string
                  requireExplicitActuation:
                    default: true
                    description: requireExplicitActuation requires each NodePowerAgent
                      to opt into real host shutdown.
                    type: boolean
                type: object
              shutdownTiers:
                description: shutdownTiers configures numbered shutdown-tier policy
                  for planner ordering.
                properties:
                  defaultTier:
                    description: defaultTier is assigned to targets that match no
                      explicit tier. Tiers 0 and 1 are reserved and cannot be the
                      default.
                    format: int32
                    minimum: 2
                    type: integer
                  labelKey:
                    description: labelKey is the Kubernetes label key whose numeric
                      value declares an object's shutdown tier.
                    type: string
                  selectorRules:
                    description: selectorRules assign tiers by selector before falling
                      back to defaultTier.
                    items:
                      description: PowerShutdownTierSelectorRule assigns a tier to
                        objects matched by labels.
                      properties:
                        name:
                          description: name is unique within the policy.
                          minLength: 1
                          type: string
                        selector:
                          description: selector matches object labels.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        subject:
                          description: subject limits which target kind this rule
                            applies to. Empty means Any.
                          enum:
                          - Any
                          - Namespace
                          - Workload
                          - Node
                          type: string
                        tier:
                          description: tier is assigned to matching objects. Tier
                            0 is workload-only and cannot be assigned to nodes.
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - name
                      - selector
                      - tier
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  tiers:
                    description: tiers documents the known tier numbers and their
                      operator-facing meanings.
                    items:
                      description: PowerShutdownTierDefinition names a shutdown tier.
                      properties:
                        description:
                          description: description explains the tier's purpose.
                          type: string
                        name:
                          description: name is a stable human-readable tier name.
                          type: string
                        tier:
                          description: tier is the numeric shutdown tier. Higher tiers
                            stop earlier; lower tiers stop later.
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - tier
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - tier
                    x-kubernetes-list-type: map
                type: object
              storage:
                description: storage configures durable audit, telemetry, and flow
                  execution state.
                properties:
                  auditSpool:
                    description: |-
                      auditSpool enables a local JSONL fallback for audit records generated while PostgreSQL is unavailable during shutdown execution.
                      The configured path must be backed by a durable volume supplied by the deployment.
                    properties:
                      enabled:
                        default: false
                        description: enabled allows execution to continue when PostgreSQL
                          writes fail after the audit store was opened.
                        type: boolean
                      maxSize:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          maxSize caps the spool journal, expressed as a Kubernetes quantity such as 64Mi.
                          A PostgreSQL outage has no bounded duration, so an uncapped journal grows until the
                          durable volume behind spec.storage.auditSpool.path is full. Once the cap is reached the
                          operator stops spooling and reports the loss on the ShutdownFlow rather than filling the
                          volume during a power event. Defaults to 64Mi.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      path:
                        description: path is the absolute in-container directory where
                          JSONL spool records are appended.
                        maxLength: 512
                        type: string
                    type: object
                  cnpg:
                    description: cnpg points at a CloudNativePG Cluster used for storage.
                    properties:
                      appCredentialSecretRef:
                        description: appCredentialSecretRef points at credentials
                          generated or accepted for the app user.
                        properties:
                          name:
                            description: name is the referenced object's metadata.name.
                            minLength: 1
                            type: string
                          namespace:
                            description: namespace is the referenced object's namespace.
                            minLength: 1
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      clusterRef:
                        description: clusterRef references a postgresql.cnpg.io Cluster
                          object.
                        properties:
                          name:
                            description: name is the referenced object's metadata.name.
                            minLength: 1
                            type: string
                          namespace:
                            description: namespace is the referenced object's namespace.
                            minLength: 1
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      database:
                        default: power
                        description: database is the application database name.
                        type: string
                      schema:
                        default: power
                        description: schema is the PostgreSQL schema used by this
                          operator.
                        type: string
                    required:
                    - clusterRef
                    type: object
                  externalPostgres:
                    description: externalPostgres points at an existing PostgreSQL
                      database.
                    properties:
                      dsnSecretKeyRef:
                        description: dsnSecretKeyRef contains a PostgreSQL DSN.
                        properties:
                          key:
                            description: key is the data key inside the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the Secret name.
                            minLength: 1
                            type: string
                          namespace:
                            description: namespace is the Secret namespace.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      requireTLS:
                        default: true
                        description: requireTLS requires PostgreSQL TLS from operator-managed
                          clients.
                        type: boolean
                      schema:
                        default: power
                        description: schema is the PostgreSQL schema used by this
                          operator.
                        type: string
                    required:
                    - dsnSecretKeyRef
                    type: object
                  mode:
                    default: CNPG
                    description: mode selects the storage backend. Disabled is suitable
                      only for development.
                    enum:
                    - Disabled
                    - ExternalPostgres
                    - CNPG
                    type: string
                  retention:
                    description: retention controls how long audit and telemetry records
                      are retained.
                    properties:
                      events:
                        description: events keeps power events and operator decisions
                          for this duration.
                        type: string
                      telemetry:
                        description: telemetry keeps raw UPS telemetry snapshots for
                          this duration.
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: status defines the observed state of PowerManagementCluster
            properties:
              conditions:
                description: conditions represent the current state of the PowerManagementCluster
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              managedResources:
                description: managedResources lists top-level resources produced for
                  this control plane.
                items:
                  description: ManagedResourceStatus records one Kubernetes resource
                    managed for a CR.
                  properties:
                    apiVersion:
                      description: apiVersion is the managed resource apiVersion.
                      type: string
                    hash:
                      description: hash is the rendered configuration hash, when applicable.
                      type: string
                    kind:
                      description: kind is the managed resource kind.
                      type: string
                    name:
                      description: name is the managed resource name.
                      type: string
                    namespace:
                      description: namespace is the managed resource namespace, when
                        namespaced.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              readyAgentFleets:
                description: readyAgentFleets is the number of NodePowerAgent resources
                  reporting Ready.
                format: int32
                type: integer
              readyServers:
                description: readyServers is the number of NUTServer resources reporting
                  Ready.
                format: int32
                type: integer
              storage:
                description: storage reports durable storage readiness.
                properties:
                  message:
                    description: message summarizes the current storage state.
                    type: string
                  mode:
                    description: mode is the observed storage mode.
                    enum:
                    - Disabled
                    - ExternalPostgres
                    - CNPG
                    type: string
                  ready:
                    description: ready reports whether durable storage is usable.
                    type: boolean
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: shutdownflows.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: ShutdownFlow
    listKind: ShutdownFlowList
    plural: shutdownflows
    singular: shutdownflow
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ShutdownFlow is the Schema for the shutdownflows API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ShutdownFlow
            properties:
              abortPolicy:
                description: abortPolicy defines behavior after a failed or unsafe
                  step.
                properties:
                  behavior:
                    default: HaltAndSurface
                    description: behavior controls what happens after abort.
                    enum:
                    - HaltAndSurface
                    - ContinueSafeSteps
                    type: string
                  notify:
                    default: true
                    description: notify enables notification steps after abort.
                    type: boolean
                type: object
              concurrencyPolicy:
                default: Forbid
                description: concurrencyPolicy controls overlapping flow evaluations.
                enum:
                - Forbid
                - Replace
                type: string
              groups:
                description: groups define the dependency graph used to compile ordered
                  shutdown waves.
                items:
                  description: ShutdownGroup defines a shutdown subject and its graph
                    relationships.
                  properties:
                    action:
                      description: action selects the operation applied to the selected
                        target.
                      enum:
                      - Notify
                      - Wait
                      - Gate
                      - CordonNodes
                      - DrainNodes
                      - ScaleWorkload
                      - RunWorkflow
                      - AgentShutdown
                      type: string
                    after:
                      description: after names groups that must complete before this
                        group can begin.
                      items:
                        type: string
                      type: array
                    before:
                      description: before names groups that cannot begin until this
                        group has completed.
                      items:
                        type: string
                      type: array
                    description:
                      description: description explains the operator-facing purpose
                        of the group.
                      type: string
                    name:
                      description: name is unique within the flow and is referenced
                        by dependency fields.
                      minLength: 1
                      type: string
                    params:
                      additionalProperties:
                        type: string
                      description: params holds action-specific scalar parameters.
                      type: object
                    phase:
                      description: |-
                        phase is a numeric fallback ordering hint. Lower phases are compiled earlier.
                        Explicit dependencies still take precedence.
                      format: int32
                      type: integer
                    requires:
                      description: |-
                        requires names groups that must remain available while this group shuts down.
                        During shutdown this group is ordered before the groups it requires.
                      items:
                        type: string
                      type: array
                    shutdownTier:
                      description: shutdownTier assigns this group to a numbered shutdown
                        tier. Higher tiers stop earlier; tier 0 cannot be targeted.
                      format: int32
                      minimum: 0
                      type: integer
                    target:
                      description: target selects the resources affected by this group.
                      properties:
                        agentRefs:
                          description: agentRefs selects NodePowerAgent fleets for
                            AgentShutdown steps.
                          items:
                            description: ObjectNameReference references another cluster-scoped
                              power.zalud.io object by name.
                            properties:
                              name:
                                description: name is the referenced object's metadata.name.
                                minLength: 1
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                        namespaceSelector:
                          description: namespaceSelector selects namespaces for namespace-oriented
                            steps.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        namespaces:
                          description: namespaces selects namespaces for namespace-oriented
                            steps.
                          items:
                            type: string
                          type: array
                        nodeSelector:
                          description: nodeSelector selects Nodes for node-oriented
                            steps.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        workloadRefs:
                          description: workloadRefs selects specific workloads.
                          items:
                            description: WorkloadReference identifies a namespaced
                              Kubernetes workload.
                            properties:
                              apiVersion:
                                description: apiVersion is the workload apiVersion.
                                minLength: 1
                                type: string
                              kind:
                                description: kind is the workload kind.
                                minLength: 1
                                type: string
                              name:
                                description: name is the workload name.
                                minLength: 1
                                type: string
                              namespace:
                                description: namespace is the workload namespace.
                                minLength: 1
                                type: string
                            required:
                            - apiVersion
                            - kind
                            - name
                            - namespace
                            type: object
                          type: array
                        workloadSelector:
                          description: workloadSelector selects workloads by labels
                            within selected namespaces.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    tierInversionPolicy:
                      default: Block
                      description: |-
                        tierInversionPolicy decides what happens when this group runs on a node whose own
                        shutdown tier would power that node off while this group is still expected to be
                        running. Block withholds the node from power-off for the whole flow, so the flow
                        shuts down less than planned rather than pulling power from live work. Allow powers
                        the node off on schedule and accepts that this group goes down with it.
                      enum:
                      - Block
                      - Allow
                      type: string
                    timeout:
                      description: timeout limits how long the group may run.
                      type: string
                  required:
                  - action
                  - name
                  type: object
                type: array
              managementClusterRef:
                description: managementClusterRef references the owning PowerManagementCluster.
                properties:
                  name:
                    description: name is the referenced object's metadata.name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              mode:
                default: DryRun
                description: mode controls whether the flow only compiles/evaluates
                  or may execute actions.
                enum:
                - DryRun
                - Enforce
                type: string
              safety:
                description: safety defines global safety gates for this flow.
                properties:
                  allowUnidentifiedDevices:
                    default: false
                    description: |-
                      allowUnidentifiedDevices permits Enforce mode when a UPS in scope matched
                      no product capability profile and fell through to the unidentified-device
                      profile.

                      Defaults to false, and that default is the point. An unidentified device
                      has had nothing verified about it: the operator knows only that some NUT
                      driver answered. UPS hardware varies too much for that to be a safe basis
                      for cutting power to real nodes, so enforcement is blocked until either a
                      profile matches or an operator states, in Git, that they accept it.
                    type: boolean
                  approvalAnnotation:
                    description: approvalAnnotation is the annotation key accepted
                      as manual approval.
                    type: string
                  maxEstimatedDuration:
                    description: maxEstimatedDuration rejects compiled plans that
                      exceed this budget.
                    type: string
                  requireManualApproval:
                    default: true
                    description: requireManualApproval requires an approval annotation
                      before Enforce mode can execute.
                    type: boolean
                type: object
              steps:
                description: steps define a linear fallback plan for simple installs.
                items:
                  description: ShutdownStep defines one ordered flow action.
                  properties:
                    continueOnError:
                      default: false
                      description: continueOnError allows later steps to continue
                        after this step fails.
                      type: boolean
                    description:
                      description: description explains the operator-facing purpose
                        of the step.
                      type: string
                    duration:
                      description: duration is used by Wait steps.
                      type: string
                    id:
                      description: id is unique within the flow.
                      minLength: 1
                      type: string
                    params:
                      additionalProperties:
                        type: string
                      description: params holds step-type-specific scalar parameters.
                      type: object
                    target:
                      description: target selects nodes, namespaces, or workloads
                        depending on step type.
                      properties:
                        agentRefs:
                          description: agentRefs selects NodePowerAgent fleets for
                            AgentShutdown steps.
                          items:
                            description: ObjectNameReference references another cluster-scoped
                              power.zalud.io object by name.
                            properties:
                              name:
                                description: name is the referenced object's metadata.name.
                                minLength: 1
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                        namespaceSelector:
                          description: namespaceSelector selects namespaces for namespace-oriented
                            steps.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        namespaces:
                          description: namespaces selects namespaces for namespace-oriented
                            steps.
                          items:
                            type: string
                          type: array
                        nodeSelector:
                          description: nodeSelector selects Nodes for node-oriented
                            steps.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        workloadRefs:
                          description: workloadRefs selects specific workloads.
                          items:
                            description: WorkloadReference identifies a namespaced
                              Kubernetes workload.
                            properties:
                              apiVersion:
                                description: apiVersion is the workload apiVersion.
                                minLength: 1
                                type: string
                              kind:
                                description: kind is the workload kind.
                                minLength: 1
                                type: string
                              name:
                                description: name is the workload name.
                                minLength: 1
                                type: string
                              namespace:
                                description: namespace is the workload namespace.
                                minLength: 1
                                type: string
                            required:
                            - apiVersion
                            - kind
                            - name
                            - namespace
                            type: object
                          type: array
                        workloadSelector:
                          description: workloadSelector selects workloads by labels
                            within selected namespaces.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    timeout:
                      description: timeout limits how long the step may run.
                      type: string
                    type:
                      description: type selects the action implementation.
                      enum:
                      - Notify
                      - Wait
                      - Gate
                      - CordonNodes
                      - DrainNodes
                      - ScaleWorkload
                      - RunWorkflow
                      - AgentShutdown
                      type: string
                  required:
                  - id
                  - type
                  type: object
                type: array
              triggers:
                description: triggers define when this flow becomes eligible.
                items:
                  description: ShutdownTrigger defines when a flow is eligible to
                    run.
                  properties:
                    chargeBelowPercent:
                      description: chargeBelowPercent is used by ChargeBelow triggers.
                      format: int32
                      maximum: 100
                      minimum: 0
                      type: integer
                    for:
                      description: for requires the trigger condition to hold for
                        this duration.
                      type: string
                    powerDomains:
                      description: powerDomains limits this trigger to named power
                        domains.
                      items:
                        type: string
                      type: array
                    runtimeBelowSeconds:
                      description: runtimeBelowSeconds is used by RuntimeBelow triggers.
                      format: int64
                      minimum: 0
                      type: integer
                    type:
                      description: type is the trigger kind.
                      enum:
                      - OnBattery
                      - LowBattery
                      - RuntimeBelow
                      - ChargeBelow
                      - TelemetryStale
                      type: string
                    upsDeviceRefs:
                      description: upsDeviceRefs limits this trigger to specific UPSDevice
                        resources.
                      items:
                        description: ObjectNameReference references another cluster-scoped
                          power.zalud.io object by name.
                        properties:
                          name:
                            description: name is the referenced object's metadata.name.
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                      type: array
                  required:
                  - type
                  type: object
                minItems: 1
                type: array
            required:
            - triggers
            type: object
          status:
            description: status defines the observed state of ShutdownFlow
            properties:
              blockedNodeReleases:
                description: |-
                  blockedNodeReleases names nodes this flow will not power off because a group still
                  expected to be running is scheduled on them (OD-18). They are published because a
                  flow that quietly shuts down fewer nodes than its plan implies is worse than one
                  that says which nodes it is leaving up and why.
                items:
                  description: |-
                    BlockedNodeReleaseStatus is one node the flow will not power off, and the
                    group whose tier caused it to be held back.
                  properties:
                    groups:
                      description: |-
                        groups names the shutdown groups running on this node that are scheduled to
                        outlive it.
                      items:
                        type: string
                      type: array
                    message:
                      description: message explains the block in operator-facing terms.
                      type: string
                    nodeName:
                      description: nodeName is the cluster node withheld from power-off.
                      minLength: 1
                      type: string
                    nodeTier:
                      description: nodeTier is the node's own declared shutdown tier.
                      format: int32
                      type: integer
                    reason:
                      description: reason is the machine-readable cause, currently
                        always ShutdownTierInversion.
                      type: string
                  required:
                  - nodeName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - nodeName
                x-kubernetes-list-type: map
              capabilityMatchCount:
                description: capabilityMatchCount records how many UPS devices received
                  capability profile matches.
                format: int32
                type: integer
              compiledSteps:
                description: compiledSteps is the controller-compiled view of the
                  flow for pre-flight review.
                items:
                  description: CompiledShutdownStep is the status-visible compiled
                    form of one flow step.
                  properties:
                    cumulativeDuration:
                      description: cumulativeDuration is the expected elapsed time
                        through this step.
                      type: string
                    id:
                      description: id is the source step id.
                      type: string
                    index:
                      description: index is the zero-based compiled order.
                      format: int32
                      type: integer
                    shutdownTier:
                      description: shutdownTier is the effective numbered shutdown
                        tier, when present.
                      format: int32
                      type: integer
                    targetSummary:
                      description: targetSummary summarizes selected targets without
                        storing bulky object lists.
                      type: string
                    type:
                      description: type is the source step type.
                      enum:
                      - Notify
                      - Wait
                      - Gate
                      - CordonNodes
                      - DrainNodes
                      - ScaleWorkload
                      - RunWorkflow
                      - AgentShutdown
                      type: string
                  required:
                  - id
                  - index
                  - type
                  type: object
                type: array
              compiledWaves:
                description: compiledWaves is the dependency-graph execution plan
                  grouped into ordered waves.
                items:
                  description: CompiledShutdownWave is the status-visible execution
                    form of shutdown groups.
                  properties:
                    cumulativeDuration:
                      description: cumulativeDuration is the expected elapsed time
                        through this wave.
                      type: string
                    duration:
                      description: duration is the maximum expected duration of any
                        group in this wave.
                      type: string
                    groups:
                      description: groups are the shutdown group names in this wave.
                      items:
                        type: string
                      type: array
                    index:
                      description: index is the zero-based wave order.
                      format: int32
                      type: integer
                    phase:
                      description: phase is the phase hint shared by this wave, when
                        one was present.
                      format: int32
                      type: integer
                    shutdownTier:
                      description: shutdownTier is the numbered tier shared by this
                        wave, when tier policy assigned one.
                      format: int32
                      type: integer
                  required:
                  - groups
                  - index
                  type: object
                type: array
              conditions:
                description: conditions represent the current state of the ShutdownFlow
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: configHash identifies the compiled plan.
                type: string
              estimatedDuration:
                description: estimatedDuration is the cumulative expected duration
                  of the compiled plan.
                type: string
              inventoryEdgeCount:
                description: inventoryEdgeCount records how many declarative inventory
                  edges fed this plan.
                format: int32
                type: integer
              inventoryEntityCount:
                description: inventoryEntityCount records how many declarative inventory
                  entities fed this plan.
                format: int32
                type: integer
              lastEvaluationTime:
                description: lastEvaluationTime is the most recent policy evaluation.
                format: date-time
                type: string
              lastExecution:
                description: lastExecution summarizes the most recent executor handoff
                  and stores the active-trigger dedupe key.
                properties:
                  actionAttemptCount:
                    description: actionAttemptCount records how many action attempts
                      were recorded.
                    format: int32
                    type: integer
                  completedAt:
                    description: completedAt records when execution evidence recording
                      finished.
                    format: date-time
                    type: string
                  deduplicationKey:
                    description: deduplicationKey identifies the active trigger episode
                      and compiled plan this execution covers.
                    type: string
                  dryRun:
                    description: dryRun is true when the executor suppressed all effectful
                      actions.
                    type: boolean
                  executionID:
                    description: executionID identifies the durable execution record.
                    type: string
                  groupCount:
                    description: groupCount records how many groups produced execution
                      evidence.
                    format: int32
                    type: integer
                  message:
                    description: message is a human-readable execution summary.
                    type: string
                  mode:
                    description: mode is the flow mode observed at execution time.
                    enum:
                    - DryRun
                    - Enforce
                    type: string
                  nodeReleaseCount:
                    description: nodeReleaseCount records how many node release decisions
                      were recorded.
                    format: int32
                    type: integer
                  phase:
                    description: phase summarizes the executor result.
                    enum:
                    - Running
                    - Completed
                    - Aborted
                    - Failed
                    - Skipped
                    type: string
                  planConfigHash:
                    description: planConfigHash is the compiled plan hash executed.
                    type: string
                  reason:
                    description: reason explains why the execution was started or
                      skipped.
                    type: string
                  selectedUPSDevices:
                    description: selectedUPSDevices is the sorted union of UPS devices
                      that made the trigger eligible.
                    items:
                      type: string
                    type: array
                  startedAt:
                    description: startedAt records when execution evidence recording
                      began.
                    format: date-time
                    type: string
                  triggerActive:
                    description: triggerActive is true while the trigger episode represented
                      by deduplicationKey remains eligible.
                    type: boolean
                  waveCount:
                    description: waveCount records how many compiled waves were handed
                      to the executor.
                    format: int32
                    type: integer
                required:
                - dryRun
                - triggerActive
                type: object
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes the current flow state.
                enum:
                - Pending
                - Compiled
                - Blocked
                - Running
                - Aborted
                - Completed
                - Error
                type: string
              publishedArtifact:
                description: publishedArtifact is the structured planner artifact
                  published for subscribers.
                properties:
                  diagrams:
                    description: diagrams are deterministic renderings generated from
                      graph.
                    properties:
                      d2:
                        description: d2 is the D2 rendering.
                        type: string
                      graphvizDOT:
                        description: graphvizDOT is the Graphviz DOT rendering.
                        type: string
                      mermaid:
                        description: mermaid is the Mermaid flowchart rendering.
                        type: string
                    type: object
                  explanations:
                    description: explanations record planner and edge-level "why"
                      statements.
                    items:
                      description: PlannerExplanationStatus is a publishable planner
                        "why" statement.
                      properties:
                        id:
                          description: id is the stable explanation id.
                          type: string
                        message:
                          description: message is the human-readable explanation.
                          type: string
                        reason:
                          description: reason is a machine-readable explanation reason.
                          type: string
                        subject:
                          description: subject identifies the graph edge, wave, or
                            planner object explained.
                          type: string
                      required:
                      - id
                      - message
                      type: object
                    type: array
                  graph:
                    description: graph is the normalized dependency graph.
                    properties:
                      edges:
                        description: edges are directed dependencies from from to
                          to.
                        items:
                          description: PlannerGraphEdgeStatus is one normalized graph
                            edge.
                          properties:
                            explanation:
                              description: explanation is the stable publishable reason
                                for this edge.
                              type: string
                            from:
                              description: from is the source graph vertex id.
                              type: string
                            id:
                              description: id is the stable graph edge id.
                              type: string
                            provenance:
                              description: provenance records whether the edge was
                                declared, derived, or policy-produced.
                              type: string
                            relation:
                              description: relation is the planner relation that produced
                                this edge.
                              type: string
                            sources:
                              description: sources identify the declarative fields
                                that produced this edge.
                              items:
                                description: PlannerGraphSourceRefStatus identifies
                                  a declarative source for a graph edge.
                                properties:
                                  field:
                                    description: field identifies the source input
                                      field.
                                    type: string
                                  kind:
                                    description: kind identifies the source input
                                      kind.
                                    type: string
                                  name:
                                    description: name identifies the source input
                                      object or item.
                                    type: string
                                type: object
                              type: array
                            to:
                              description: to is the destination graph vertex id.
                              type: string
                          required:
                          - from
                          - id
                          - to
                          type: object
                        type: array
                      vertices:
                        description: vertices are graph nodes.
                        items:
                          description: PlannerGraphVertexStatus is one normalized
                            graph vertex.
                          properties:
                            action:
                              description: action is the compiled shutdown action
                                associated with the vertex.
                              type: string
                            id:
                              description: id is the stable graph vertex id.
                              type: string
                            kind:
                              description: kind identifies the source vertex kind.
                              type: string
                            label:
                              description: label is the human-readable vertex label.
                              type: string
                            phase:
                              description: phase is the shutdown phase hint, when
                                present.
                              format: int32
                              type: integer
                            shutdownTier:
                              description: shutdownTier is the effective numbered
                                shutdown tier, when present.
                              format: int32
                              type: integer
                            targetSummary:
                              description: targetSummary summarizes selected targets
                                without storing bulky object lists.
                              type: string
                          required:
                          - id
                          type: object
                        type: array
                    type: object
                  startupWaves:
                    description: startupWaves is the advisory reverse-order projection
                      for subscriber-owned recovery.
                    items:
                      description: CompiledShutdownWave is the status-visible execution
                        form of shutdown groups.
                      properties:
                        cumulativeDuration:
                          description: cumulativeDuration is the expected elapsed
                            time through this wave.
                          type: string
                        duration:
                          description: duration is the maximum expected duration of
                            any group in this wave.
                          type: string
                        groups:
                          description: groups are the shutdown group names in this
                            wave.
                          items:
                            type: string
                          type: array
                        index:
                          description: index is the zero-based wave order.
                          format: int32
                          type: integer
                        phase:
                          description: phase is the phase hint shared by this wave,
                            when one was present.
                          format: int32
                          type: integer
                        shutdownTier:
                          description: shutdownTier is the numbered tier shared by
                            this wave, when tier policy assigned one.
                          format: int32
                          type: integer
                      required:
                      - groups
                      - index
                      type: object
                    type: array
                type: object
              resolvedInputHash:
                description: resolvedInputHash identifies the declarative inventory
                  and UPS capability inputs used by the compiled plan.
                type: string
              topologyHash:
                description: topologyHash identifies the compiled declarative power
                  and communication topology.
                type: string
              triggerEvaluation:
                description: triggerEvaluation is the most recent trigger evaluation
                  against UPS telemetry status.
                properties:
                  decisions:
                    description: decisions records one decision per configured trigger.
                    items:
                      description: ShutdownTriggerDecisionStatus records the evaluation
                        result for one trigger.
                      properties:
                        eligible:
                          description: eligible is true when the telemetry condition
                            is true and hold-time gating has elapsed.
                          type: boolean
                        eligibleAt:
                          description: eligibleAt records when the hold duration is
                            satisfied.
                          format: date-time
                          type: string
                        holdStartedAt:
                          description: holdStartedAt records when this trigger/device
                            condition first became true.
                          format: date-time
                          type: string
                        matched:
                          description: matched is true when the telemetry condition
                            is currently true before hold-time gating.
                          type: boolean
                        reason:
                          description: reason explains the decision.
                          type: string
                        selectedUPSDevices:
                          description: selectedUPSDevices names UPS devices matched
                            by this trigger.
                          items:
                            type: string
                          type: array
                        triggerID:
                          description: triggerID is the controller-generated stable
                            identifier for this trigger position.
                          type: string
                        type:
                          description: type is the trigger kind.
                          enum:
                          - OnBattery
                          - LowBattery
                          - RuntimeBelow
                          - ChargeBelow
                          - TelemetryStale
                          type: string
                      required:
                      - eligible
                      - matched
                      type: object
                    type: array
                  diagnostics:
                    description: diagnostics records non-fatal warnings or trigger
                      definition errors surfaced by evaluation.
                    items:
                      description: ShutdownTriggerDiagnosticStatus records a trigger-evaluation
                        warning or error.
                      properties:
                        message:
                          description: message is the human-readable diagnostic.
                          type: string
                        reason:
                          description: reason is a machine-readable diagnostic reason.
                          type: string
                        severity:
                          description: severity is Warning or Error.
                          type: string
                        subject:
                          description: subject identifies the affected trigger or
                            UPS device.
                          type: string
                      required:
                      - message
                      - reason
                      - severity
                      type: object
                    type: array
                  eligible:
                    description: eligible is true when at least one trigger is eligible
                      to proceed to the executor boundary.
                    type: boolean
                  matchedTriggerCount:
                    description: matchedTriggerCount records how many triggers matched
                      telemetry before hold-time gating.
                    format: int32
                    type: integer
                  mode:
                    description: mode is the flow mode at evaluation time.
                    enum:
                    - DryRun
                    - Enforce
                    type: string
                  observedAt:
                    description: observedAt is the controller-supplied time used for
                      the evaluation.
                    format: date-time
                    type: string
                  planConfigHash:
                    description: planConfigHash is the compiled plan hash evaluated
                      with this trigger tick.
                    type: string
                  reason:
                    description: reason is the high-level evaluation reason.
                    type: string
                  selectedUPSDevices:
                    description: selectedUPSDevices is the sorted union of UPS devices
                      selected by eligible triggers.
                    items:
                      type: string
                    type: array
                required:
                - eligible
                type: object
              triggerHoldStates:
                description: triggerHoldStates persists compact hold timers for triggers
                  with spec.triggers[].for.
                items:
                  description: ShutdownTriggerHoldStateStatus persists one active
                    trigger hold timer.
                  properties:
                    startedAt:
                      description: startedAt records when the condition first became
                        true.
                      format: date-time
                      type: string
                    triggerID:
                      description: triggerID identifies the trigger whose condition
                        is being held.
                      type: string
                    upsDevice:
                      description: upsDevice is the UPSDevice name whose condition
                        is being held.
                      type: string
                  required:
                  - startedAt
                  - triggerID
                  - upsDevice
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: upscapabilityprobes.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: UPSCapabilityProbe
    listKind: UPSCapabilityProbeList
    plural: upscapabilityprobes
    singular: upscapabilityprobe
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deviceRef.name
      name: Device
      type: string
    - jsonPath: .status.identity.model
      name: Model
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.probedAt
      name: Probed
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UPSCapabilityProbe is the Schema for the upscapabilityprobes
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of UPSCapabilityProbe
            properties:
              deviceRef:
                description: |-
                  deviceRef names the UPSDevice to probe. The device must already be
                  selected by a ready NUTServer, since the probe reads through the same
                  NUT endpoint telemetry polling uses.
                properties:
                  name:
                    description: name is the UPSDevice name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              profileName:
                description: |-
                  profileName is the metadata.name to use in the drafted profile. Defaults
                  to a name derived from the reported manufacturer and model.
                type: string
              refreshInterval:
                description: |-
                  refreshInterval re-runs the probe on a schedule. Leave unset for a
                  one-shot probe that only re-runs when the spec changes.
                type: string
            required:
            - deviceRef
            type: object
          status:
            description: status defines the observed state of UPSCapabilityProbe
            properties:
              conditions:
                description: conditions represent the current state of the UPSCapabilityProbe
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentMatch:
                description: |-
                  currentMatch records which profile this device resolves to today, so the
                  gap the probe is filling is visible without a second lookup.
                properties:
                  profileID:
                    description: profileID is the profile the device resolves to today.
                    type: string
                  tier:
                    description: tier is the precedence tier that matched.
                    type: string
                  unidentified:
                    description: |-
                      unidentified is true when the device matched no product profile and fell
                      through to the unidentified-device profile.
                    type: boolean
                type: object
              draftProfile:
                description: |-
                  draftProfile is a ready-to-apply UPSCapabilityProfile manifest. Review it
                  before applying: the actuation section is intentionally empty.
                type: string
              identity:
                description: |-
                  identity is the product identity the device reported. Serial numbers are
                  deliberately not recorded: they identify one unit, not a product model.
                properties:
                  firmware:
                    description: firmware is the reported ups.firmware value.
                    type: string
                  manufacturer:
                    description: manufacturer is the reported ups.mfr value.
                    type: string
                  model:
                    description: model is the reported ups.model value and the profile's
                      match key.
                    type: string
                type: object
              issueReport:
                description: |-
                  issueReport is the same findings formatted for pasting into a GitHub
                  issue, so a verified profile can be contributed back to the catalog.
                type: string
              nonStandardVariables:
                description: |-
                  nonStandardVariables are reported names that are not standard NUT names.
                  They are worth a human look and may deserve an alias or a quirk.
                items:
                  type: string
                type: array
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes probe progress.
                enum:
                - Pending
                - Probed
                - Failed
                type: string
              probedAt:
                description: probedAt is when the device was last read.
                format: date-time
                type: string
              reportedVariables:
                description: reportedVariables are the NUT variable names the device
                  exposed.
                items:
                  type: string
                type: array
              suggestedAliases:
                additionalProperties:
                  type: string
                description: |-
                  suggestedAliases are proposed alias mappings, keyed by the non-standard
                  name the device reported, where a standard reading appears to have arrived
                  under it. Suggestions only, in the same shape spec.telemetry.aliases takes.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: upscapabilityprofiles.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: UPSCapabilityProfile
    listKind: UPSCapabilityProfileList
    plural: upscapabilityprofiles
    singular: upscapabilityprofile
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UPSCapabilityProfile is the Schema for the upscapabilityprofiles
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of UPSCapabilityProfile
            properties:
              actuation:
                description: actuation declares authoritative UPS control behaviors
                  exposed by matching devices.
                properties:
                  behaviors:
                    description: behaviors are authoritative actuation behavior identifiers.
                    items:
                      type: string
                    type: array
                type: object
              quirks:
                description: quirks records behavior-affecting quirks consumed by
                  planner or executor policy.
                items:
                  type: string
                type: array
              selector:
                description: selector declares which UPS devices this profile can
                  match.
                properties:
                  driverFamily:
                    description: driverFamily matches the NUT driver family, such
                      as snmp-ups.
                    type: string
                  firmware:
                    description: firmware narrows an exact model match to a specific
                      firmware string.
                    type: string
                  model:
                    description: model matches an exact provider model string.
                    type: string
                  modelGlob:
                    description: modelGlob matches a provider model string using path.Match
                      glob semantics.
                    type: string
                  universal:
                    default: false
                    description: universal marks this profile as the unidentified-device
                      fallback.
                    type: boolean
                type: object
              telemetry:
                description: telemetry declares authoritative NUT variables exposed
                  by matching devices.
                properties:
                  aliases:
                    additionalProperties:
                      type: string
                    description: |-
                      aliases map a non-standard variable name the device reports (the key) onto
                      the canonical NUT name it should be read as (the value). A canonical name
                      the device reports natively always wins over an alias, and every applied
                      alias is recorded in telemetry diagnostics.

                      A map rather than a list of pairs: a reported name can only ever resolve
                      one way, so making a duplicate key unrepresentable is better than
                      validating against it.
                    type: object
                  variables:
                    description: variables are authoritative NUT variable names exposed
                      by matching devices.
                    items:
                      type: string
                    type: array
                type: object
              version:
                description: version is the semantic version of this behavior profile.
                minLength: 1
                type: string
            required:
            - selector
            - version
            type: object
          status:
            description: status defines the observed state of UPSCapabilityProfile
            properties:
              conditions:
                description: conditions represent the current state of the UPSCapabilityProfile
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes profile contract acceptance.
                enum:
                - Accepted
                - Error
                type: string
              profileHash:
                description: profileHash identifies the accepted capability profile
                  content.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: upsdevices.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: UPSDevice
    listKind: UPSDeviceList
    plural: upsdevices
    singular: upsdevice
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UPSDevice is the Schema for the upsdevices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of UPSDevice
            properties:
              credentialSecretRef:
                description: credentialSecretRef points at driver credentials, such
                  as SNMP community or SNMPv3 auth material.
                properties:
                  name:
                    description: name is the referenced object's metadata.name.
                    minLength: 1
                    type: string
                  namespace:
                    description: namespace is the referenced object's namespace.
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              displayName:
                description: displayName is a human-readable name for events and published
                  artifacts.
                type: string
              driver:
                description: |-
                  driver is the Network UPS Tools driver name for a network-capable UPS driver,
                  such as snmp-ups, netxml-ups, powerman-pdu, apcupsd-ups, or dummy-ups for
                  tests. It is required unless upstreamNUT is set. Local USB and serial drivers
                  are intentionally unsupported by this API.
                type: string
              driverOptions:
                additionalProperties:
                  type: string
                description: driverOptions is rendered into ups.conf for this device.
                type: object
              endpoint:
                description: |-
                  endpoint describes the network endpoint reached by the NUT driver. It is not
                  a USB, serial, or host device path. Use upstreamNUT instead for devices that
                  already expose their own NUT data server.
                properties:
                  host:
                    description: host is the DNS name or IP address reached by the
                      NUT driver.
                    minLength: 1
                    type: string
                  port:
                    description: port is the driver-specific network port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - host
                type: object
              identity:
                description: |-
                  identity declares stable capability matching keys when they are known from
                  inventory, a trusted probe, or a vendor-reported NUT model string.
                properties:
                  firmware:
                    description: firmware narrows matching to a specific provider-reported
                      firmware string.
                    type: string
                  model:
                    description: model is the provider-reported NUT ups.model value
                      or trusted inventory equivalent.
                    type: string
                type: object
              powerDomains:
                description: powerDomains names the physical power domains this UPS
                  supplies.
                items:
                  type: string
                type: array
              simulation:
                description: |-
                  simulation configures scripted state-transition fixture data for the dummy-ups driver, used
                  to exercise OnBattery/LowBattery transitions in tests without real hardware. Only valid when
                  driver is dummy-ups and upstreamNUT is not set.
                properties:
                  sequenceConfigMapRef:
                    description: |-
                      sequenceConfigMapRef points at a ConfigMap holding a dummy-ups `.seq` scripted
                      transition fixture (NUT's TIMER-directive format: blocks of `variable: value` lines,
                      optionally preceded by a `TIMER <seconds>` line, separated by blank lines, looping once the
                      sequence ends). Must be in the rendered NUTServer operand namespace, matching
                      credentialSecretRef's convention.
                    properties:
                      name:
                        description: name is the referenced object's metadata.name.
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the referenced object's namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  sequenceKey:
                    default: sequence.seq
                    description: sequenceKey is the ConfigMap data key holding the
                      .seq fixture content.
                    type: string
                required:
                - sequenceConfigMapRef
                type: object
              telemetry:
                description: telemetry controls device polling and stale-data behavior.
                properties:
                  alertPollInterval:
                    description: alertPollInterval controls polling while the UPS
                      is on battery or otherwise degraded.
                    type: string
                  pollInterval:
                    description: pollInterval controls normal-state telemetry polling.
                    type: string
                type: object
              thresholds:
                description: thresholds defines policy-relevant UPS thresholds.
                properties:
                  minChargePercent:
                    description: minChargePercent is the minimum battery charge before
                      the UPS is considered critical.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  minRuntimeSeconds:
                    description: minRuntimeSeconds is the minimum remaining runtime
                      before the UPS is considered critical.
                    format: int64
                    minimum: 0
                    type: integer
                  onBatteryHoldoff:
                    description: onBatteryHoldoff delays action for short transfer
                      events.
                    type: string
                  staleAfter:
                    description: staleAfter marks telemetry stale after this duration
                      without a successful poll.
                    type: string
                type: object
              upstreamNUT:
                description: |-
                  upstreamNUT describes a UPS that already exposes a network NUT server. The
                  generated NUTServer relays it through dummy-ups repeater mode.
                properties:
                  auth:
                    description: |-
                      auth configures how dummy-ups repeater mode discovers authentication and
                      trust material for the upstream NUT server.
                    properties:
                      mode:
                        default: None
                        description: |-
                          mode selects the upstream authconf source. The default disables authconf
                          parsing so unauthenticated appliances do not depend on image defaults.
                        enum:
                        - None
                        - Default
                        - Secret
                        type: string
                      secretKeyRef:
                        description: |-
                          secretKeyRef points at a Secret key containing a complete nutauth.conf file.
                          The Secret must be in the rendered NUTServer operand namespace.
                        properties:
                          key:
                            description: key is the data key inside the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the Secret name.
                            minLength: 1
                            type: string
                          namespace:
                            description: namespace is the Secret namespace.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                    type: object
                  host:
                    description: host is the DNS name or IP address of the upstream
                      upsd server.
                    minLength: 1
                    type: string
                  port:
                    default: 3493
                    description: port is the upstream upsd TCP port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  strictStart:
                    default: true
                    description: |-
                      strictStart preserves dummy-ups repeater mode's strict startup behavior.
                      When false, startup continues even if the upstream server is unavailable.
                    type: boolean
                  upsName:
                    description: upsName is the upstream UPS name served by the upstream
                      upsd server.
                    minLength: 1
                    type: string
                required:
                - host
                - upsName
                type: object
            type: object
          status:
            description: status defines the observed state of UPSDevice
            properties:
              batteryChargePercent:
                description: batteryChargePercent is the last observed battery.charge
                  value.
                format: int32
                type: integer
              conditions:
                description: conditions represent the current state of the UPSDevice
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastPollTime:
                description: lastPollTime is the most recent successful telemetry
                  poll.
                format: date-time
                type: string
              lastStatus:
                description: lastStatus is the last observed NUT ups.status value.
                type: string
              loadPercent:
                description: loadPercent is the last observed ups.load value.
                format: int32
                type: integer
              nutName:
                description: nutName is the rendered NUT device name.
                type: string
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes the current observed device state.
                enum:
                - Unknown
                - Online
                - OnBattery
                - LowBattery
                - Stale
                - Unavailable
                type: string
              runtimeSeconds:
                description: runtimeSeconds is the last observed battery.runtime value.
                format: int64
                type: integer
              serverRefs:
                description: serverRefs names NUTServer resources currently serving
                  this device.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-controller-manager
  namespace: nut-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-leader-election-role
  namespace: nut-operator-system
rules:
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: nut-operator-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  - namespaces
  - secrets
  - serviceaccounts
  - services
  verbs:
  - create
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - ""
  resources:
  - nodes
  verbs:
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - pods/eviction
  verbs:
  - create
- apiGroups:
  - apps
  resources:
  - daemonsets
  - deployments
  verbs:
  - create
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps
  resources:
  - replicasets
  - statefulsets
  verbs:
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - workflows
  verbs:
  - create
  - get
  - list
  - watch
- apiGroups:
  - networking.k8s.io
  resources:
  - networkpolicies
  verbs:
  - create
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - policy
  resources:
  - poddisruptionbudgets
  verbs:
  - create
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - postgresql.cnpg.io
  resources:
  - clusters
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents
  - nutservers
  - pducapabilityprofiles
  - powerinfrastructures
  - powerinventoryedges
  - powerinventorynodes
  - powermanagementclusters
  - shutdownflows
  - upscapabilityprobes
  - upscapabilityprofiles
  - upsdevices
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents/finalizers
  - nutservers/finalizers
  - pducapabilityprofiles/finalizers
  - powerinfrastructures/finalizers
  - powerinventoryedges/finalizers
  - powerinventorynodes/finalizers
  - powermanagementclusters/finalizers
  - shutdownflows/finalizers
  - upscapabilityprobes/finalizers
  - upscapabilityprofiles/finalizers
  - upsdevices/finalizers
  verbs:
  - update
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents/status
  - nutservers/status
  - pducapabilityprofiles/status
  - powerinfrastructures/status
  - powerinventoryedges/status
  - powerinventorynodes/status
  - powermanagementclusters/status
  - shutdownflows/status
  - upscapabilityprobes/status
  - upscapabilityprofiles/status
  - upsdevices/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: nut-operator-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: nut-operator-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-nodepoweragent-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-nodepoweragent-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-nodepoweragent-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nodepoweragents/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-nutserver-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - nutservers
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nutservers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-nutserver-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - nutservers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nutservers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-nutserver-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - nutservers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - nutservers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinfrastructure-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinfrastructures
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinfrastructures/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinfrastructure-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinfrastructures
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinfrastructures/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinfrastructure-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinfrastructures
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinfrastructures/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinventoryedge-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventoryedges
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventoryedges/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinventoryedge-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventoryedges
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventoryedges/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinventoryedge-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventoryedges
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventoryedges/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinventorynode-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventorynodes
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventorynodes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinventorynode-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventorynodes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventorynodes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powerinventorynode-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventorynodes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powerinventorynodes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powermanagementcluster-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powermanagementclusters
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powermanagementclusters/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powermanagementcluster-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powermanagementclusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powermanagementclusters/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-powermanagementcluster-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - powermanagementclusters
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - powermanagementclusters/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-shutdownflow-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - shutdownflows
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - shutdownflows/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-shutdownflow-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - shutdownflows
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - shutdownflows/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-shutdownflow-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - shutdownflows
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - shutdownflows/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upscapabilityprobe-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprobes
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprobes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upscapabilityprobe-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprobes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprobes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upscapabilityprobe-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprobes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprobes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upscapabilityprofile-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprofiles
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprofiles/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upscapabilityprofile-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprofiles
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprofiles/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upscapabilityprofile-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprofiles
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upscapabilityprofiles/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upsdevice-admin-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upsdevices
  verbs:
  - create
  - delete
  - deletecollection
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upsdevices/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upsdevice-editor-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upsdevices
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upsdevices/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-upsdevice-viewer-role
rules:
- apiGroups:
  - power.zalud.io
  resources:
  - upsdevices
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - power.zalud.io
  resources:
  - upsdevices/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-leader-election-rolebinding
  namespace: nut-operator-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: nut-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: nut-operator-controller-manager
  namespace: nut-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: nut-operator-manager-role
subjects:
- kind: ServiceAccount
  name: nut-operator-controller-manager
  namespace: nut-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: nut-operator-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: nut-operator-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: nut-operator-controller-manager
  namespace: nut-operator-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
    control-plane: controller-manager
  name: nut-operator-controller-manager-metrics-service
  namespace: nut-operator-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: nut-operator
    control-plane: controller-manager
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-webhook-service
  namespace: nut-operator-system
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app.kubernetes.io/name: nut-operator
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    checkov.io/skip1: CKV_K8S_43=Base manager image is replaced by Kustomize, release
      workflows, or GitOps overlays; production deployments use immutable digests.
    checkov.io/skip2: CKV_K8S_38=The controller manager requires a Kubernetes API
      service account token to reconcile CRDs and owned resources.
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
    control-plane: controller-manager
  name: nut-operator-controller-manager
  namespace: nut-operator-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nut-operator
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: nut-operator
        control-plane: controller-manager
    spec:
      automountServiceAccountToken: true
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        - --webhook-cert-path=/tmp/k8s-webhook-server/serving-certs
        command:
        - /manager
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: ghcr.io/michaelzalud18/nut-operator:main
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 8081
          name: health
          protocol: TCP
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsGroup: 65532
          runAsUser: 65532
        volumeMounts:
        - mountPath: /tmp/k8s-webhook-server/serving-certs
          name: webhook-certs
          readOnly: true
      priorityClassName: system-cluster-critical
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: nut-operator-controller-manager
      terminationGracePeriodSeconds: 10
      volumes:
      - name: webhook-certs
        secret:
          secretName: webhook-server-cert
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
    control-plane: controller-manager
  name: nut-operator-controller-manager
  namespace: nut-operator-system
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nut-operator
      control-plane: controller-manager
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-metrics-certs
  namespace: nut-operator-system
spec:
  dnsNames:
  - SERVICE_NAME.SERVICE_NAMESPACE.svc
  - SERVICE_NAME.SERVICE_NAMESPACE.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: nut-operator-selfsigned-issuer
  secretName: metrics-server-cert
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-serving-cert
  namespace: nut-operator-system
spec:
  dnsNames:
  - nut-operator-webhook-service.nut-operator-system.svc
  - nut-operator-webhook-service.nut-operator-system.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: nut-operator-selfsigned-issuer
  secretName: webhook-server-cert
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-selfsigned-issuer
  namespace: nut-operator-system
spec:
  selfSigned: {}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-allow-metrics-traffic
  namespace: nut-operator-system
spec:
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          metrics: enabled
    ports:
    - port: 8443
      protocol: TCP
  podSelector:
    matchLabels:
      app.kubernetes.io/name: nut-operator
      control-plane: controller-manager
  policyTypes:
  - Ingress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: nut-operator
  name: nut-operator-allow-webhook-traffic
  namespace: nut-operator-system
spec:
  ingress:
  - ports:
    - port: 9443
      protocol: TCP
  podSelector:
    matchLabels:
      app.kubernetes.io/name: nut-operator
      control-plane: controller-manager
  policyTypes:
  - Ingress
---
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: nut-operator-system/nut-operator-serving-cert
  name: nut-operator-mutating-webhook-configuration
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-nodepoweragent
  failurePolicy: Fail
  name: mnodepoweragent-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - nodepoweragents
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-nutserver
  failurePolicy: Fail
  name: mnutserver-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - nutservers
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-powerinfrastructure
  failurePolicy: Fail
  name: mpowerinfrastructure-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powerinfrastructures
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-powerinventoryedge
  failurePolicy: Fail
  name: mpowerinventoryedge-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powerinventoryedges
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-powerinventorynode
  failurePolicy: Fail
  name: mpowerinventorynode-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powerinventorynodes
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-powermanagementcluster
  failurePolicy: Fail
  name: mpowermanagementcluster-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powermanagementclusters
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-shutdownflow
  failurePolicy: Fail
  name: mshutdownflow-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - shutdownflows
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-upscapabilityprofile
  failurePolicy: Fail
  name: mupscapabilityprofile-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - upscapabilityprofiles
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /mutate-power-zalud-io-v1alpha1-upsdevice
  failurePolicy: Fail
  name: mupsdevice-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - upsdevices
  sideEffects: None
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: nut-operator-system/nut-operator-serving-cert
  name: nut-operator-validating-webhook-configuration
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-nodepoweragent
  failurePolicy: Fail
  name: vnodepoweragent-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - nodepoweragents
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-nutserver
  failurePolicy: Fail
  name: vnutserver-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - nutservers
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-powerinfrastructure
  failurePolicy: Fail
  name: vpowerinfrastructure-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powerinfrastructures
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-powerinventoryedge
  failurePolicy: Fail
  name: vpowerinventoryedge-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powerinventoryedges
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-powerinventorynode
  failurePolicy: Fail
  name: vpowerinventorynode-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powerinventorynodes
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-powermanagementcluster
  failurePolicy: Fail
  name: vpowermanagementcluster-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - powermanagementclusters
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-shutdownflow
  failurePolicy: Fail
  name: vshutdownflow-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - shutdownflows
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-upscapabilityprofile
  failurePolicy: Fail
  name: vupscapabilityprofile-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - upscapabilityprofiles
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: nut-operator-webhook-service
      namespace: nut-operator-system
      path: /validate-power-zalud-io-v1alpha1-upsdevice
  failurePolicy: Fail
  name: vupsdevice-v1alpha1.kb.io
  rules:
  - apiGroups:
    - power.zalud.io
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - upsdevices
  sideEffects: None
