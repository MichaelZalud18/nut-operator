---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: shutdownhooks.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: ShutdownHook
    listKind: ShutdownHookList
    plural: shutdownhooks
    singular: shutdownhook
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ShutdownHook is the Schema for the shutdownhooks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ShutdownHook
            properties:
              dryRun:
                description: |-
                  dryRun is an optional author-declared rehearsal invocation. If omitted,
                  dry-run records the request that would have been sent without contacting
                  the target system.
                properties:
                  http:
                    description: http declares the CloudEvents-over-HTTP invocation.
                    properties:
                      cloudEventSource:
                        description: |-
                          cloudEventSource overrides the CloudEvents source. Empty uses the
                          ShutdownFlow name at execution time.
                        type: string
                      cloudEventType:
                        description: cloudEventType overrides the CloudEvents type.
                          Empty uses the operator default.
                        type: string
                      data:
                        additionalProperties:
                          type: string
                        description: data adds static string fields under the CloudEvents
                          data.extensions object.
                        type: object
                      headers:
                        description: |-
                          headers are non-secret HTTP headers. Sensitive headers such as
                          Authorization must be supplied through secretHeaders.
                        items:
                          description: ShutdownHookHTTPHeader is one non-secret HTTP
                            header.
                          properties:
                            name:
                              description: name is the HTTP header name.
                              minLength: 1
                              type: string
                            value:
                              description: value is the literal header value.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      method:
                        default: POST
                        description: |-
                          method is the HTTP method. POST is the only supported method for v1alpha1
                          hooks so receivers get one predictable contract.
                        enum:
                        - POST
                        type: string
                      secretHeaders:
                        description: secretHeaders are HTTP headers whose values are
                          read from Secrets.
                        items:
                          description: ShutdownHookHTTPSecretHeader is one HTTP header
                            sourced from a Secret key.
                          properties:
                            name:
                              description: name is the HTTP header name.
                              minLength: 1
                              type: string
                            valueFrom:
                              description: valueFrom identifies the Secret key containing
                                the header value.
                              properties:
                                key:
                                  description: key is the data key inside the Secret.
                                  minLength: 1
                                  type: string
                                name:
                                  description: name is the Secret name.
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: namespace is the Secret namespace.
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              - namespace
                              type: object
                          required:
                          - name
                          - valueFrom
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      url:
                        description: url is the endpoint that receives the CloudEvents-shaped
                          JSON body.
                        minLength: 1
                        type: string
                    required:
                    - url
                    type: object
                  kubernetesObject:
                    description: |-
                      kubernetesObject declares a raw Kubernetes object to create. This is a
                      generic transport, not an Argo-specific workflow path; the install must
                      grant RBAC for the selected GVK before using it.
                    properties:
                      object:
                        description: |-
                          object is the Kubernetes object body to create. The object must carry
                          apiVersion, kind, metadata, and either metadata.name or metadata.generateName.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - object
                    type: object
                  timeout:
                    default: 10s
                    description: |-
                      timeout limits this hook delivery attempt. The executor may compress the
                      effective timeout when battery runtime forces faster wave timing.
                    type: string
                  transport:
                    default: HTTP
                    description: transport selects how the hook is delivered.
                    enum:
                    - HTTP
                    - KubernetesObject
                    type: string
                type: object
              failurePolicy:
                default: MarkDegraded
                description: |-
                  failurePolicy controls how a failed hook is surfaced. Hooks are advisory:
                  they never hold a shutdown wave and never engage abortPolicy.
                enum:
                - MarkDegraded
                type: string
              invocation:
                description: invocation is the request sent during an enforce-mode
                  shutdown.
                properties:
                  http:
                    description: http declares the CloudEvents-over-HTTP invocation.
                    properties:
                      cloudEventSource:
                        description: |-
                          cloudEventSource overrides the CloudEvents source. Empty uses the
                          ShutdownFlow name at execution time.
                        type: string
                      cloudEventType:
                        description: cloudEventType overrides the CloudEvents type.
                          Empty uses the operator default.
                        type: string
                      data:
                        additionalProperties:
                          type: string
                        description: data adds static string fields under the CloudEvents
                          data.extensions object.
                        type: object
                      headers:
                        description: |-
                          headers are non-secret HTTP headers. Sensitive headers such as
                          Authorization must be supplied through secretHeaders.
                        items:
                          description: ShutdownHookHTTPHeader is one non-secret HTTP
                            header.
                          properties:
                            name:
                              description: name is the HTTP header name.
                              minLength: 1
                              type: string
                            value:
                              description: value is the literal header value.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      method:
                        default: POST
                        description: |-
                          method is the HTTP method. POST is the only supported method for v1alpha1
                          hooks so receivers get one predictable contract.
                        enum:
                        - POST
                        type: string
                      secretHeaders:
                        description: secretHeaders are HTTP headers whose values are
                          read from Secrets.
                        items:
                          description: ShutdownHookHTTPSecretHeader is one HTTP header
                            sourced from a Secret key.
                          properties:
                            name:
                              description: name is the HTTP header name.
                              minLength: 1
                              type: string
                            valueFrom:
                              description: valueFrom identifies the Secret key containing
                                the header value.
                              properties:
                                key:
                                  description: key is the data key inside the Secret.
                                  minLength: 1
                                  type: string
                                name:
                                  description: name is the Secret name.
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: namespace is the Secret namespace.
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              - namespace
                              type: object
                          required:
                          - name
                          - valueFrom
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      url:
                        description: url is the endpoint that receives the CloudEvents-shaped
                          JSON body.
                        minLength: 1
                        type: string
                    required:
                    - url
                    type: object
                  kubernetesObject:
                    description: |-
                      kubernetesObject declares a raw Kubernetes object to create. This is a
                      generic transport, not an Argo-specific workflow path; the install must
                      grant RBAC for the selected GVK before using it.
                    properties:
                      object:
                        description: |-
                          object is the Kubernetes object body to create. The object must carry
                          apiVersion, kind, metadata, and either metadata.name or metadata.generateName.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - object
                    type: object
                  timeout:
                    default: 10s
                    description: |-
                      timeout limits this hook delivery attempt. The executor may compress the
                      effective timeout when battery runtime forces faster wave timing.
                    type: string
                  transport:
                    default: HTTP
                    description: transport selects how the hook is delivered.
                    enum:
                    - HTTP
                    - KubernetesObject
                    type: string
                type: object
            required:
            - invocation
            type: object
          status:
            description: status defines the observed state of ShutdownHook
            properties:
              conditions:
                description: conditions represent the current state of the ShutdownHook
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
