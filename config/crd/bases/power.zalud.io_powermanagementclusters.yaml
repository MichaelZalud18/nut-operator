---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: powermanagementclusters.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: PowerManagementCluster
    listKind: PowerManagementClusterList
    plural: powermanagementclusters
    singular: powermanagementcluster
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PowerManagementCluster is the Schema for the powermanagementclusters
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PowerManagementCluster
            properties:
              hooks:
                description: hooks configures global policy for ShutdownHook delivery.
                properties:
                  allowedEndpoints:
                    description: |-
                      allowedEndpoints is the HTTP endpoint allowlist for ShutdownHook invocation.
                      HTTP hooks that do not match an entry are refused before delivery.
                    items:
                      description: PowerHookEndpointAllowlistEntry allows one HTTP
                        hook endpoint shape.
                      properties:
                        host:
                          description: host is the exact DNS name or IP literal allowed
                            for hook delivery.
                          minLength: 1
                          type: string
                        pathPrefix:
                          description: pathPrefix limits delivery to paths under this
                            prefix. Empty allows any path.
                          type: string
                        port:
                          description: port is the allowed TCP port. Empty means the
                            scheme default port.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        scheme:
                          description: scheme is the allowed URL scheme.
                          enum:
                          - http
                          - https
                          type: string
                      required:
                      - host
                      - scheme
                      type: object
                    type: array
                  defaultTimeout:
                    default: 10s
                    description: defaultTimeout is used when a hook invocation does
                      not declare its own timeout.
                    type: string
                type: object
              images:
                description: images provides default images for generated operands.
                properties:
                  actuator:
                    description: actuator is the minimal node-actuation image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  exporter:
                    description: exporter is the metrics exporter image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  nutServer:
                    description: nutServer is the upsd and driver image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  operator:
                    description: operator is the controller-manager image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                  upsmonAgent:
                    description: upsmonAgent is the unprivileged NUT client image.
                    properties:
                      digest:
                        description: digest is an optional immutable image digest.
                        type: string
                      pullPolicy:
                        description: pullPolicy controls when the kubelet pulls the
                          image.
                        type: string
                      repository:
                        description: repository is the image repository, without the
                          tag.
                        type: string
                      tag:
                        description: tag is the image tag. Production installs should
                          prefer digest pinning in packaging overlays.
                        type: string
                    type: object
                type: object
              inventory:
                description: inventory configures how declarative inventory snapshots
                  are treated.
                properties:
                  snapshotAgeLevels:
                    description: |-
                      snapshotAgeLevels raise the reported severity as an inventory snapshot ages
                      (IN-16). Age never blocks planning: a provider outage must degrade rather
                      than stop a shutdown (IN-15), so the worst an old snapshot can do is say so
                      loudly. The levels exist because "last good snapshot" must not be able to
                      quietly mean "eight months ago".

                      Thresholds are evaluated highest-first, so the oldest matching entry wins.
                      An empty list applies the defaults: Info at one hour, Warning at six.
                    items:
                      description: PowerSnapshotAgeLevel raises one severity once
                        a snapshot reaches an age.
                      properties:
                        after:
                          description: after is the snapshot age at which this level
                            begins to apply.
                          pattern: ^([0-9]+(\.[0-9]+)?(ns|us|ms|s|m|h))+$
                          type: string
                        level:
                          description: |-
                            level is the severity reported once a snapshot is older than after. Info is
                            recorded and published without changing conditions; Warning additionally
                            degrades flows compiled from the snapshot.
                          enum:
                          - Info
                          - Warning
                          type: string
                      required:
                      - after
                      - level
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - level
                    x-kubernetes-list-type: map
                type: object
              observability:
                description: observability configures metrics, events, and telemetry
                  export defaults.
                properties:
                  kubernetesEvents:
                    default: true
                    description: kubernetesEvents enables Kubernetes event emission
                      for power state changes.
                    type: boolean
                  publishCadence:
                    description: |-
                      publishCadence bounds how long the operator may publish nothing before republishing
                      unchanged state, so a subscriber can tell "nothing is happening" from "the publisher died"
                      (EX-29). Both look like silence otherwise.

                      Cluster-wide rather than per-flow (OD-30): the cadence describes the publisher's liveness,
                      and there is one publisher. A subscriber watching several flows needs one interval to
                      reason about, and letting each flow set its own would make the reconcile rate a workload
                      author's decision. Per-flow variation that is actually justified already exists as the
                      idle/active split below, which follows what a flow is doing rather than what it declared.
                    properties:
                      active:
                        description: |-
                          active is the republish interval while a flow is mid-outage — descending, or holding a
                          matched trigger. Defaults to 10s. Must be faster than idle: the whole point of the split is
                          that a subscriber tracking progress needs fresher state than one confirming liveness.
                        type: string
                      idle:
                        description: idle is the republish interval when no flow is
                          mid-outage. Defaults to 60s.
                        type: string
                    type: object
                  serviceMonitor:
                    default: true
                    description: serviceMonitor enables Prometheus Operator ServiceMonitor
                      rendering.
                    type: boolean
                  telemetryInterval:
                    description: telemetryInterval is the default interval for durable
                      UPS telemetry snapshots.
                    type: string
                type: object
              operandNamespace:
                description: operandNamespace defines where generated Deployments,
                  DaemonSets, Services, ConfigMaps, and Secrets live.
                properties:
                  create:
                    default: true
                    description: |-
                      create lets the operator create the namespace if it does not exist. Set it to false to keep
                      ownership of the namespace's existence -- its quotas, its admission labels, its lifecycle --
                      with the cluster administrator; the operator then labels a namespace that is already there
                      and reports the operand namespace missing rather than creating it.
                    type: boolean
                  name:
                    default: power-system
                    description: name is the namespace for generated workloads and
                      Services.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              security:
                description: security sets global security defaults for generated
                  operands.
                properties:
                  allowedActuatorNamespaces:
                    description: allowedActuatorNamespaces restricts where host-actuator
                      DaemonSets may be created.
                    items:
                      type: string
                    type: array
                  defaultPodHardening:
                    description: defaultPodHardening applies to generated non-actuator
                      containers.
                    properties:
                      readOnlyRootFilesystem:
                        default: true
                        description: readOnlyRootFilesystem requests read-only container
                          root filesystems.
                        type: boolean
                      runAsNonRoot:
                        default: true
                        description: runAsNonRoot requests non-root execution where
                          the operand supports it.
                        type: boolean
                      seccompProfileType:
                        default: RuntimeDefault
                        description: seccompProfileType is the seccomp profile type
                          for generated pods.
                        enum:
                        - RuntimeDefault
                        - Localhost
                        - Unconfined
                        type: string
                    type: object
                  profile:
                    default: Restricted
                    description: profile selects the operator's workload hardening
                      baseline.
                    enum:
                    - Restricted
                    - HostActuatorIsolated
                    type: string
                  requireExplicitActuation:
                    default: true
                    description: requireExplicitActuation requires each NodePowerAgent
                      to opt into real host shutdown.
                    type: boolean
                type: object
              shutdownTiers:
                description: shutdownTiers configures numbered shutdown-tier policy
                  for planner ordering.
                properties:
                  defaultTier:
                    description: defaultTier is assigned to targets that match no
                      explicit tier. Tiers 0 and 1 are reserved and cannot be the
                      default.
                    format: int32
                    minimum: 2
                    type: integer
                  labelKey:
                    description: labelKey is the Kubernetes label key whose numeric
                      value declares an object's shutdown tier.
                    type: string
                  selectorRules:
                    description: selectorRules assign tiers by selector before falling
                      back to defaultTier.
                    items:
                      description: PowerShutdownTierSelectorRule assigns a tier to
                        objects matched by labels.
                      properties:
                        name:
                          description: name is unique within the policy.
                          minLength: 1
                          type: string
                        selector:
                          description: selector matches object labels.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        subject:
                          description: subject limits which target kind this rule
                            applies to. Empty means Any.
                          enum:
                          - Any
                          - Namespace
                          - Workload
                          - Node
                          type: string
                        tier:
                          description: tier is assigned to matching objects. Tier
                            0 is workload-only and cannot be assigned to nodes.
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - name
                      - selector
                      - tier
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  tiers:
                    description: tiers documents the known tier numbers and their
                      operator-facing meanings.
                    items:
                      description: PowerShutdownTierDefinition names a shutdown tier.
                      properties:
                        description:
                          description: description explains the tier's purpose.
                          type: string
                        name:
                          description: name is a stable human-readable tier name.
                          type: string
                        tier:
                          description: tier is the numeric shutdown tier. Higher tiers
                            stop earlier; lower tiers stop later.
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - tier
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - tier
                    x-kubernetes-list-type: map
                type: object
              storage:
                description: storage configures durable audit, telemetry, and flow
                  execution state.
                properties:
                  auditSpool:
                    description: |-
                      auditSpool enables a local JSONL fallback for audit records generated while PostgreSQL is unavailable during shutdown execution.
                      The configured path must be backed by a durable volume supplied by the deployment.
                    properties:
                      enabled:
                        default: false
                        description: enabled allows execution to continue when PostgreSQL
                          writes fail after the audit store was opened.
                        type: boolean
                      maxSize:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          maxSize caps the spool journal, expressed as a Kubernetes quantity such as 64Mi.
                          A PostgreSQL outage has no bounded duration, so an uncapped journal grows until the
                          durable volume behind spec.storage.auditSpool.path is full. Once the cap is reached the
                          operator stops spooling and reports the loss on the ShutdownFlow rather than filling the
                          volume during a power event. Defaults to 64Mi.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      path:
                        description: path is the absolute in-container directory where
                          JSONL spool records are appended.
                        maxLength: 512
                        type: string
                    type: object
                  cnpg:
                    description: cnpg points at a CloudNativePG Cluster used for storage.
                    properties:
                      appCredentialSecretRef:
                        description: appCredentialSecretRef points at credentials
                          generated or accepted for the app user.
                        properties:
                          name:
                            description: name is the referenced object's metadata.name.
                            minLength: 1
                            type: string
                          namespace:
                            description: namespace is the referenced object's namespace.
                            minLength: 1
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      clusterRef:
                        description: clusterRef references a postgresql.cnpg.io Cluster
                          object.
                        properties:
                          name:
                            description: name is the referenced object's metadata.name.
                            minLength: 1
                            type: string
                          namespace:
                            description: namespace is the referenced object's namespace.
                            minLength: 1
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      database:
                        default: power
                        description: database is the application database name.
                        type: string
                      schema:
                        default: power
                        description: schema is the PostgreSQL schema used by this
                          operator.
                        type: string
                    required:
                    - clusterRef
                    type: object
                  externalPostgres:
                    description: externalPostgres points at an existing PostgreSQL
                      database.
                    properties:
                      dsnSecretKeyRef:
                        description: dsnSecretKeyRef contains a PostgreSQL DSN.
                        properties:
                          key:
                            description: key is the data key inside the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the Secret name.
                            minLength: 1
                            type: string
                          namespace:
                            description: namespace is the Secret namespace.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      requireTLS:
                        default: true
                        description: requireTLS requires PostgreSQL TLS from operator-managed
                          clients.
                        type: boolean
                      schema:
                        default: power
                        description: schema is the PostgreSQL schema used by this
                          operator.
                        type: string
                    required:
                    - dsnSecretKeyRef
                    type: object
                  mode:
                    default: CNPG
                    description: mode selects the storage backend. Disabled is suitable
                      only for development.
                    enum:
                    - Disabled
                    - ExternalPostgres
                    - CNPG
                    type: string
                  retention:
                    description: retention controls how long audit and telemetry records
                      are retained.
                    properties:
                      events:
                        description: events keeps power events and operator decisions
                          for this duration.
                        type: string
                      telemetry:
                        description: telemetry keeps raw UPS telemetry snapshots for
                          this duration.
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: status defines the observed state of PowerManagementCluster
            properties:
              conditions:
                description: conditions represent the current state of the PowerManagementCluster
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              managedResources:
                description: managedResources lists top-level resources produced for
                  this control plane.
                items:
                  description: ManagedResourceStatus records one Kubernetes resource
                    managed for a CR.
                  properties:
                    apiVersion:
                      description: apiVersion is the managed resource apiVersion.
                      type: string
                    hash:
                      description: hash is the rendered configuration hash, when applicable.
                      type: string
                    kind:
                      description: kind is the managed resource kind.
                      type: string
                    name:
                      description: name is the managed resource name.
                      type: string
                    namespace:
                      description: namespace is the managed resource namespace, when
                        namespaced.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              readyAgentFleets:
                description: readyAgentFleets is the number of NodePowerAgent resources
                  reporting Ready.
                format: int32
                type: integer
              readyServers:
                description: readyServers is the number of NUTServer resources reporting
                  Ready.
                format: int32
                type: integer
              storage:
                description: storage reports durable storage readiness.
                properties:
                  message:
                    description: message summarizes the current storage state.
                    type: string
                  mode:
                    description: mode is the observed storage mode.
                    enum:
                    - Disabled
                    - ExternalPostgres
                    - CNPG
                    type: string
                  ready:
                    description: ready reports whether durable storage is usable.
                    type: boolean
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
