---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: powerinventorynodes.power.zalud.io
spec:
  group: power.zalud.io
  names:
    kind: PowerInventoryNode
    listKind: PowerInventoryNodeList
    plural: powerinventorynodes
    singular: powerinventorynode
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PowerInventoryNode is the Schema for the powerinventorynodes
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PowerInventoryNode
            properties:
              communicationPathExempt:
                default: false
                description: communicationPathExempt suppresses missing carries-path
                  warnings for this node.
                type: boolean
              nodeName:
                description: nodeName is the canonical Kubernetes Node metadata.name
                  this inventory record describes.
                minLength: 1
                type: string
              powerPlanningExempt:
                default: false
                description: powerPlanningExempt excludes this node from the UPS feeds
                  orphan rule.
                type: boolean
              roles:
                description: roles declares planner-relevant node roles.
                properties:
                  controlPlane:
                    default: false
                    description: controlPlane marks this node as hosting Kubernetes
                      control-plane responsibilities.
                    type: boolean
                  controlPlaneQuorumMember:
                    default: false
                    description: controlPlaneQuorumMember marks this node as counting
                      toward control-plane quorum.
                    type: boolean
                  lastDitchRole:
                    description: |-
                      lastDitchRole is an explicit late-shutdown role interpreted by planner policy.
                      When shutdownTier is omitted, any non-empty lastDitchRole maps this node to tier 1.
                    type: string
                  shutdownTier:
                    description: shutdownTier explicitly assigns this node to a numbered
                      shutdown tier. Nodes cannot use tier 0.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
            required:
            - nodeName
            type: object
          status:
            description: status defines the observed state of PowerInventoryNode
            properties:
              conditions:
                description: conditions represent the current state of the PowerInventoryNode
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the last generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: phase summarizes inventory contract acceptance.
                enum:
                - Accepted
                - Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
